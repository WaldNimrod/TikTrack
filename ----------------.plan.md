# שיפור עמודת פעולות בטבלאות - Actions Menu Popup

## סקירה

במקום רשימת כפתורים בעמודת הפעולות, יהיה איקון אחד (⋮) שעל hover יפתח פופאפ מעוצב Material Design עם כל הכפתורים.

## מצב נוכחי

- **מערכת כללית:** `button-icons.js` מספקת `createEditButton()`, `createDeleteButton()`, וכו'
- **יצירת HTML:** כל עמוד (trades.js, tickers.js, executions.js) יוצר בעצמו את עמודת הפעולות
- **עיצוב נוכחי:** כפתורים בשורה, כל כפתור 28x28px עם margin 2px
- **CSS:** `styles-new/06-components/_tables.css` מגדיר `.actions-cell`
- **צבעים:** מוגדרים ב-`_colors-dynamic.css` ו-`_buttons-advanced.css`

## תיקונים מהדוגמה (demo-actions-menu.html)

כל התיקונים הבאים אושרו על ידי המשתמש:

1. ✅ **מיקום פופאפ:** `top: 50%` + `translateY(-50%)` + `margin-left: 2px`
2. ✅ **תיקון בעיית רווח:** pseudo-element `::before` עם `right: -8px`
3. ✅ **צבע טריגר:** `var(--primary-color, #26baac)` 
4. ✅ **גדלים:** כפתורים 28x28px (לא 32px)
5. ✅ **margin:** `0 2px` בין כפתורים
6. ✅ **מסגרות:** כל כפתור עם `border: 1px solid var(--color)`
7. ✅ **ITCSS:** שימוש בסגנונות מ-`_buttons-advanced.css`

---

## שלב 1: עדכון button-icons.js

**קובץ:** `trading-ui/scripts/button-icons.js`

הוספה בסוף הקובץ (לפני `window.createActionsMenu = ...`):

```javascript
/**
 * יצירת תפריט פעולות עם popup על hover
 * @param {Array} buttons - מערך של HTML של כפתורים (תומך ב-2-5 כפתורים)
 * @param {string} entityId - ID של הישות (לייחודיות)
 * @returns {string} HTML של trigger + popup
 * 
 * דוגמאות שימוש:
 * - 2 כפתורים: createActionsMenu([editBtn, deleteBtn], id)
 * - 5 כפתורים: createActionsMenu([linkBtn, editBtn, viewBtn, cancelBtn, deleteBtn], id)
 */
function createActionsMenu(buttons, entityId) {
  // סינון כפתורים ריקים
  const validButtons = buttons.filter(btn => btn && btn.trim() !== '');
  
  if (validButtons.length === 0) {
    return '<span class="text-muted">-</span>';
  }
  
  const menuId = `actions-menu-${entityId}`;
  
  // הפופאפ יתאים אוטומטית לכמות הכפתורים (CSS flex + min-width: max-content)
  return `
    <div class="actions-menu-wrapper">
      <button class="actions-menu-trigger" 
              data-menu-id="${menuId}"
              title="פעולות"
              aria-label="תפריט פעולות">
        <span class="actions-menu-icon">⋮</span>
      </button>
      <div class="actions-menu-popup" 
           id="${menuId}"
           role="menu">
        <div class="actions-menu-content">
          ${validButtons.join('')}
        </div>
      </div>
    </div>
  `;
}

// הוספה לייצוא
window.createActionsMenu = createActionsMenu;
```

---

## שלב 2: יצירת actions-menu-system.js

**קובץ חדש:** `trading-ui/scripts/modules/actions-menu-system.js`

```javascript
/**
 * Actions Menu System
 * Manages hover-based popup menus for table actions
 * 
 * Features:
 * - Pure CSS hover (no JavaScript delays needed)
 * - Auto-positioning (RTL aware)
 * - Material Design
 */

class ActionsMenuSystem {
    constructor() {
        this.init();
    }

    init() {
        console.log('✅ Actions Menu System initialized (CSS-based hover)');
        
        // מערכת פשוטה - הכל נעשה ב-CSS עם :hover
        // ה-JavaScript רק למקרים מיוחדים (אם צריך positioning דינמי)
    }
}

// Initialize on DOM ready
if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', () => {
        window.actionsMenuSystem = new ActionsMenuSystem();
    });
} else {
    window.actionsMenuSystem = new ActionsMenuSystem();
}
```

---

## שלב 3: עדכון _tables.css

**קובץ:** `trading-ui/styles-new/06-components/_tables.css`

הוספה בסוף הקובץ:

```css
/* ============================================
   Actions Menu Popup System
   מערכת תפריט פעולות עם popup על hover
   ============================================ */

/* Wrapper - מכיל trigger + popup */
.actions-menu-wrapper {
  position: relative;
  display: inline-block;
}

/* Trigger Button - האיקון ⋮ */
.actions-menu-trigger {
  width: 32px;
  height: 32px;
  padding: 0;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: 1px solid var(--primary-color, #26baac);
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.2s ease;
  color: var(--primary-color, #26baac);
}

.actions-menu-trigger:hover {
  background: var(--primary-color, #26baac);
  color: white;
  border-color: var(--primary-color, #26baac);
}

.actions-menu-trigger:active {
  background: var(--primary-color, #26baac);
  opacity: 0.8;
}

.actions-menu-icon {
  font-size: 20px;
  line-height: 1;
  font-weight: bold;
}

/* Popup - החלון הצף */
.actions-menu-popup {
  position: absolute;
  z-index: 1000;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.2s ease, transform 0.2s ease;
  
  /* RTL: popup מצד ימין של הtrigger */
  inset-inline-start: 100%;
  top: 50%;
  transform: translateY(-50%) scale(0.95);
  margin-inline-start: 2px; /* צמוד אבל לא עולה על הכפתור */
}

/* Pseudo-element ליצירת אזור hover מורחב */
.actions-menu-popup::before {
  content: '';
  position: absolute;
  top: -8px;
  inset-inline-end: -8px; /* מכסה את הרווח לכפתור */
  bottom: -8px;
  inset-inline-start: -8px;
  z-index: -1;
}

/* Hover state - פתיחת הפופאפ */
.actions-menu-wrapper:hover .actions-menu-popup {
  opacity: 1;
  pointer-events: all;
  transform: translateY(-50%) scale(1);
}

/* Popup Content - התוכן */
.actions-menu-content {
  background: white;
  border-radius: 8px;
  padding: 6px 4px;
  box-shadow: 
    0 4px 6px -1px rgba(0, 0, 0, 0.1),
    0 2px 4px -1px rgba(0, 0, 0, 0.06),
    0 0 0 1px rgba(0, 0, 0, 0.05);
  display: flex;
  gap: 0; /* אין gap - הכפתורים עם margin 2px */
  white-space: nowrap;
  min-width: max-content;
}

/* Dark mode support (לעתיד) */
@media (prefers-color-scheme: dark) {
  .actions-menu-content {
    background: var(--apple-bg-elevated, #2c2c2e);
    box-shadow: 
      0 4px 6px -1px rgba(0, 0, 0, 0.3),
      0 2px 4px -1px rgba(0, 0, 0, 0.2),
      0 0 0 1px rgba(255, 255, 255, 0.1);
  }
}

/* Buttons inside popup - הכפתורים בפופאפ */
/* ⚠️ הכפתורים ירשו אוטומטית את הסגנונות מ-ITCSS */
.actions-menu-content button,
.actions-menu-content .btn {
  /* גדלים תקניים - תואם ל-_tables.css */
  width: 28px;
  height: 28px;
  min-width: 28px;
  max-width: 28px;
  min-height: 28px;
  max-height: 28px;
  padding: 0;
  margin: 0 2px; /* רווח תקני בין כפתורים */
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 0.75rem;
  border-radius: 8px;
  transition: all 0.15s ease;
  cursor: pointer;
  /* הצבעים והמסגרות יגיעו מ-_buttons-advanced.css */
}

.actions-menu-content button:hover,
.actions-menu-content .btn:hover {
  transform: scale(1.05);
}

/* עדכון לעמודת הפעולות - רוחב קבוע קטן */
.actions-cell {
  width: 50px !important;
  min-width: 50px !important;
  max-width: 50px !important;
  text-align: center;
  padding: 4px !important;
  overflow: visible !important;
}

/* עדכון classes ישנות של מספר כפתורים */
.actions-1-btn,
.actions-2-btn,
.actions-3-btn,
.actions-4-btn,
.actions-5-btn {
  width: 50px;
  min-width: 50px;
  max-width: 50px;
}
```

---

## שלב 4: עדכון קבצי JavaScript של טבלאות

### 4.1 trades.js

**קובץ:** `trading-ui/scripts/trades.js`  
**שורה:** ~548-554

**לפני:**
```javascript
<td class="col-actions actions-cell">
    ${window.createLinkButton ? window.createLinkButton(`viewLinkedItemsForTrade(${trade.id})`) : ''}
    ${window.createEditButton ? window.createEditButton(`editTrade(${trade.id})`) : ''}
    ${window.createButton ? window.createButton('VIEW', `viewTrade(${trade.id})`) : ''}
    ${window.createCancelButton ? window.createCancelButton('trade', trade.id, trade.status) : ''}
    ${window.createDeleteButton ? window.createDeleteButton(`deleteTrade(${trade.id})`) : ''}
</td>
```

**אחרי:**
```javascript
<td class="col-actions actions-cell">
    ${window.createActionsMenu ? window.createActionsMenu([
        window.createLinkButton ? window.createLinkButton(`viewLinkedItemsForTrade(${trade.id})`) : '',
        window.createEditButton ? window.createEditButton(`editTrade(${trade.id})`) : '',
        window.createButton ? window.createButton('VIEW', `viewTrade(${trade.id})`) : '',
        window.createCancelButton ? window.createCancelButton('trade', trade.id, trade.status) : '',
        window.createDeleteButton ? window.createDeleteButton(`deleteTrade(${trade.id})`) : ''
    ], trade.id) : ''}
</td>
```

### 4.2 db_display.js

**קובץ:** `trading-ui/scripts/db_display.js`  
**פונקציה:** `createActionsHTML()` שורה ~568-580

**לפני:**
```javascript
function createActionsHTML(row, tableType) {
  const recordId = row.id || row.ID || '';
  if (!recordId) {
    return '<span class="text-muted">-</span>';
  }

  return `
    <div class="action-buttons">
      ${window.createEditButton(`editRecord('${tableType}', ${recordId})`)}
      ${window.createDeleteButton(`deleteRecord('${tableType}', ${recordId})`)}
    </div>
  `;
}
```

**אחרי:**
```javascript
function createActionsHTML(row, tableType) {
  const recordId = row.id || row.ID || '';
  if (!recordId) {
    return '<span class="text-muted">-</span>';
  }

  const buttons = [
    window.createEditButton ? window.createEditButton(`editRecord('${tableType}', ${recordId})`) : '',
    window.createDeleteButton ? window.createDeleteButton(`deleteRecord('${tableType}', ${recordId})`) : ''
  ];

  return window.createActionsMenu ? window.createActionsMenu(buttons, recordId) : buttons.join('');
}
```

### 4.3 קבצים נוספים - אותו דפוס

עבור כל קובץ הבא, חפש את הדפוס `<td class="...actions-cell">` ועדכן לשימוש ב-`createActionsMenu()`:

1. **trade_plans.js** - שורה ~1956-1961
2. **executions.js** - שורה ~1220
3. **tickers.js** - איפה שיש כפתורי פעולות
4. **cash_flows.js** - עמודת פעולות
5. **alerts.js** - עמודת פעולות
6. **notes.js** - עמודת פעולות
7. **trading_accounts.js** - עמודת פעולות (אם קיימת)
8. **entity-details-renderer.js** - שורה ~714

---

## שלב 5: עדכון קבצי HTML

הוסף את `actions-menu-system.js` **לפני** `button-icons.js` בכל הקבצים הבאים:

```html
<!-- Actions Menu System -->
<script src="scripts/modules/actions-menu-system.js?v=HASH"></script>

<!-- Button Icons (depends on actions-menu-system) -->
<script src="scripts/button-icons.js?v=HASH"></script>
```

**קבצים לעדכון:**
1. trades.html
2. trade_plans.html
3. executions.html
4. tickers.html
5. cash_flows.html
6. alerts.html
7. notes.html
8. trading_accounts.html

---

## שלב 6: הרצת Cache Buster

אחרי כל השינויים, הרץ:

```bash
./build-tools/cache-buster.sh
```

או דרך Cursor Task: `TT: Build & Bust Cache`

זה יעדכן את ה-`?v=HASH` בכל קבצי ה-HTML.

---

## שלב 7: בדיקות

### בדיקה 1: Single Button
- פתח עמוד עם כפתור פעולה אחד
- וודא: popup עם כפתור אחד

### בדיקה 2: Multiple Buttons
- פתח עמוד trades (5 כפתורים)
- וודא: popup עם כל 5 הכפתורים

### בדיקה 3: Hover Behavior
- עבור עם העכבר על ⋮
- וודא: popup נפתח מיד
- עבור מהכפתור לפופאפ
- וודא: הפופאפ לא נסגר (אין רווח מפריע)

### בדיקה 4: Visual Check
- וודא: כפתור ⋮ בצבע טורקיז (#26baac)
- וודא: הפופאפ ממורכז מול הכפתור
- וודא: כפתורים עם מסגרות צבעוניות
- וודא: Material Design shadow על הפופאפ

### בדיקה 5: RTL
- וודא: הפופאפ נפתח מצד ימין (שמאל של הכפתור)

### בדיקה 6: כל העמודים
עבור על כל 8 העמודים ווודא שהכל עובד:
- [x] trades.html
- [x] trade_plans.html
- [x] executions.html
- [x] tickers.html
- [x] cash_flows.html
- [x] alerts.html
- [x] notes.html
- [x] trading_accounts.html

---

## שלב 8: עדכון דוקומנטציה

### 8.1 עדכן GENERAL_SYSTEMS_LIST.md

הוסף:

```markdown
### מערכת תפריט פעולות (Actions Menu System)
- **קובץ:** `actions-menu-system.js`
- **תיאור:** מנהל popup menus על hover לכפתורי פעולות בטבלאות
- **תכונות:**
  - Hover-based (ללא click, CSS pure)
  - Material Design עם shadows
  - RTL aware
  - חיסכון במקום: 75% (מ-200px ל-50px)
- **שימוש:** `createActionsMenu([buttons], entityId)`
- **גרסה:** 1.0 - ינואר 2025
```

### 8.2 צור ACTIONS_MENU_SYSTEM.md (אופציונלי)

מסמך מפורט עם:
- איך להשתמש במערכת
- דוגמאות קוד
- Troubleshooting

---

## שלב 9: גיבוי וגרסה

### 9.1 גיבוי מקומי

```bash
cd /Users/nimrod/Documents/TikTrack
tar --exclude='node_modules' --exclude='.git' --exclude='Backend/logs' \
    -czf backup-actions-menu-$(date +%Y%m%d_%H%M%S).tar.gz TikTrackApp/
```

### 9.2 Git Commit

```bash
git add .
git commit -m "feat: Actions Menu Popup System

✨ New Feature - Hover-based Actions Menu

**Changes:**
- Added createActionsMenu() to button-icons.js
- Created actions-menu-system.js (CSS-based hover)
- Updated _tables.css with popup styles
- Converted 8 pages to use actions menu
- Updated 8 HTML files with new script

**Benefits:**
- 75% space saving (200px → 50px)
- Better UX (hover only, no clicks)
- Material Design (shadows + animations)
- RTL aware
- Dynamic colors from ITCSS

**Files Changed:**
- 1 new JS module
- 1 updated system file (button-icons.js)
- 8 updated page scripts
- 8 updated HTML files
- 1 updated CSS file

**Testing:**
- ✅ All 8 pages tested
- ✅ Single/multiple buttons work
- ✅ Hover behavior smooth
- ✅ RTL positioning correct
- ✅ Colors from CSS vars

Version: 2.1.0 → 2.1.1"

git push origin master
```

---

## סיכום שינויים

### קבצים חדשים (1):
- `trading-ui/scripts/modules/actions-menu-system.js`

### קבצים מעודכנים (18):

**JavaScript (9):**
1. `button-icons.js` - פונקציה `createActionsMenu()`
2. `db_display.js` - עדכון `createActionsHTML()`
3. `trades.js` - המרה ל-actions menu
4. `trade_plans.js` - המרה ל-actions menu
5. `executions.js` - המרה ל-actions menu
6. `tickers.js` - המרה ל-actions menu
7. `cash_flows.js` - המרה ל-actions menu
8. `alerts.js` - המרה ל-actions menu
9. `notes.js` - המרה ל-actions menu

**CSS (1):**
10. `styles-new/06-components/_tables.css` - סגנונות popup

**HTML (8):**
11-18. כל קבצי ה-HTML

---

## תוצאות

✅ **חיסכון במקום:** מ-200px ל-50px בעמודת הפעולות (75%)  
✅ **UX משופר:** hover בלבד, ללא clicks מיותרים  
✅ **עיצוב מודרני:** Material Design shadows + animations  
✅ **RTL aware:** פופאפ נפתח מצד ימין  
✅ **צבעים דינאמיים:** שימוש במשתני CSS מהמערכת  
✅ **תואם ITCSS:** כל הסגנונות עוקבים אחר הארכיטקטורה

---

## To-dos

- [ ] שלב 1: עדכון button-icons.js
- [ ] שלב 2: יצירת actions-menu-system.js
- [ ] שלב 3: עדכון _tables.css
- [ ] שלב 4.1: עדכון trades.js
- [ ] שלב 4.2: עדכון db_display.js
- [ ] שלב 4.3: עדכון trade_plans.js
- [ ] שלב 4.4: עדכון executions.js
- [ ] שלב 4.5: עדכון tickers.js
- [ ] שלב 4.6: עדכון cash_flows.js
- [ ] שלב 4.7: עדכון alerts.js
- [ ] שלב 4.8: עדכון notes.js
- [ ] שלב 4.9: עדכון entity-details-renderer.js
- [ ] שלב 5: עדכון 8 קבצי HTML
- [ ] שלב 6: הרצת cache-buster.sh
- [ ] שלב 7: בדיקות מקיפות (8 עמודים)
- [ ] שלב 8: עדכון דוקומנטציה
- [ ] שלב 9: גיבוי וגרסה (commit + push)

