openapi: 3.0.0
info:
  title: TikTrack Project Phoenix API
  version: 2.54.1
paths:
  /user-api-keys:
    get:
      summary: "List Masked API Keys (D24)"
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  properties:
                    encrypted_api_keys:
                      type: string
                      example: "********************"
                      description: "STRICT MASKING ENFORCED"