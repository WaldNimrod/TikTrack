# Team 10 | S002-P002-WP003 — Nimrod GATE_7 משוב ראשוני (חלקי)

**project_domain:** TIKTRACK  
**id:** TEAM_10_NIMROD_S002_P002_WP003_G7_PRELIMINARY_FEEDBACK  
**from:** Team 10 (מתווך — מקור: Nimrod)  
**to:** Team 90, Team 30, Team 20, Team 60  
**date:** 2025-01-30  
**status:** **BLOCK** — משוב ראשוני חלקי  
**gate_id:** GATE_7  
**work_package_id:** S002-P002-WP003  

---

## 0) הודעה מקדימה

**רשימה ראשונית חלקית.** יש לבחון אותה, להציג משוב, ולהמתין לסעיפים נוספים לפני הגשת המסמך הסופי.

---

## 1) ממצאים (חלקי)

| # | ממצא | חומרה | בעלים משוער |
|---|-------|--------|--------------|
| **1** | עמודות **מקור** + **עודכן ב** מציגות **"-"** ולא נתונים | P0 | Team 30 |
| **2** | כל הטיקרים מקבלים **רמזור אדום** — חוסרים חמורים בנתונים; במודל הפרטים המצב שונה | P0 | Team 20, 30 |
| **2.5** | **לבחון:** איך סעיפים 1+2 לא נתגלו בבדיקות האוטומטיות — באגים פשוטים וברורים | P1 | Team 50, 90 |
| **3** | מודל פרטי טיקר — **לוקח המון זמן** לפתוח; חייב **אינדיקציית טעינה** מיד עם הלחיצה | P1 | Team 30 |
| **4** | לשלב במודל הפרטים ובממשק בדיקת הטיקר **כפתורים להפעלת תהליכי עדכון נתונים** — לבדיקה + תחזוקה שוטפת | P2 | Team 30, 20 |
| **5** | **מחיר נוכחי** ו**מחיר סגירה** מציגים **נתון זהה** — לא הגיוני; אם יש גם וגם — סגירה מאתמול? המערכת חייבת להבין במדויק בכל שעה ובכל מצב שוק מה המחיר האחרון ואיזה סוג; **ברור למשתמש** | P0 | Team 20, 30 |
| **6** | בורסה ת"א מחזירה מחיר **באגורות** ולא בשקלים — יש להציג בשקלים (לחלק ב-100) | P1 | Team 20, 30 |
| **7** | **כללי — כל הטבלאות:** כפתור תפריט הפעולות לא נוח; במעבר עכבר התפריט נפתח אך נסגר מהר מדי כשעדיין לא עברו לשטח הנפתח — ריווח/נקודות יציאה לא אופטימליים. תיקון: דיוק שטחים פעילים, וידוא UX, **בדיקה מלאה בדפדפן עם MCP**. | P1 | Team 30 |
| **8** | **סטטוסים של טיקרים:** לבחון שוב את האפיון (TT2_TICKER_STATUS_MARKET_DATA_LOADING_SSOT); לוודא שהקוד תואם לאפיון והממשק מציג נכון. להוסיף **מקרא קצר** אחרי הטבלה בעמוד ניהול טיקרים — משמעות סטטוס טיקר למנהל המערכת. | P2 | Team 30, 20 |
| **9** | **אלמנט סיכום מידע:** מציג נתונים לא תואמים לסטטוסים בפועל. לבחון אילו נתונים סטטיסטיים רלוונטיים נוספים להכניס בסיכום לשיפור הממשק למנהל המערכת. | P2 | Team 30, 20 |
| **10** | **עמוד ניהול מערכת** — תרחישי אוטומציה/Runtime: לוודא שניתן **לאמת ולבצע** בממשק בעמוד `http://localhost:8080/system_management.html` | P1 | Team 30, 60 |
| **11** | **היסטוריית ריצות:** לכל תהליך — אפשרות לצפייה בהיסטוריה (ע"י לוג או מודל פרטים לכל רשומה) | P2 | Team 30, 20 |
| **12** | **פריט פעולות לכל משימה:** כפתור הרצה מיידית; הפעלה/עצירה של הטימר; מודל עריכה לשינוי הגדרות וליבלים | P1 | Team 30, 20 |
| **13** | **קונטיינר Market Data:** וידוא שהמערכת פועלת לפי ההגדרות; המלצות וגבולות מדויקים; ולידציה לפני שמירה + משוב מפורט למנהל | P1 | Team 30, 20 |
| **14** | **כרטיס סיכום Market Data:** הוספה לקונטיינר — רמת עומס צפויה בהתאם להגדרות וכמות הטיקרים; **מכוון חום** | P2 | Team 30, 20 |

---

## 2) סיכום לפי צוות

| צוות | ממצאים | פעולה |
|------|--------|-------|
| **Team 30** | 1, 2 (binding), 3, 4, 5 (הצגה), 6 (הצגה), 7, 8, 9, **10, 11, 12, 13, 14** | תיקוני קוד + **עמוד ניהול מערכת: אימות/הרצה, היסטוריה, פריט פעולות, Market Data וולידציה, כרטיס סיכום** |
| **Team 20** | 2 (נתונים), 5 (לוגיקה), 6 (API/DB), 8, 9, **11, 12, 13** | וידוא נתונים; **היסטוריית ריצות; API למשימות; Market Data וולידציה** |
| **Team 50** | 2.5, 7 (MCP) | ניתוח אוטומציה; בדיקת תפריט פעולות |
| **Team 60** | **10** | וידוא תרחישי Runtime ניתנים לביצוע/אימות |
| **Team 90** | 2.5 | בחינת תרחישי QA |

---

## 3) פירוט ממצאים 7–9

### 7 — תפריט פעולות (כללי)

**בעיה:** כפתור שפותח תפריט פעולות בכל הטבלאות — במעבר עכבר (hover) התפריט נפתח, אך אם לא עוברים מהר מספיק לשטח התפריט הנפתח, הוא נסגר ולא מאפשר בחירה.

**נדרש:**
- דיוק השטחים הפעילים ונקודות היציאה
- וידוא שאין ריווח שגוי שגורם לבעיה
- UX אופטימלי
- **בדיקה מלאה בדפדפן עם MCP** לאחר התיקון

**פריסה:** `ui/src/styles/phoenix-components.css` — `.table-actions-tooltip`, `.table-actions-menu`, `:hover` / `:focus-within`. בשימוש בטבלאות: tickers, userTicker, ועוד.

---

### 8 — סטטוס טיקר ומקרא

**נדרש:**
- בחינה מחדש של האפיון (TT2_TICKER_STATUS_MARKET_DATA_LOADING_SSOT): משמעות הסטטוסים ומשמעות שינוי/ניהול
- וידוא התאמה: קוד ↔ אפיון, ממשק מציג נכון
- **מקרא קצר** אחרי הטבלה בעמוד ניהול טיקרים: משמעות כל סטטוס (ממתין / פתוח / סגור / מבוטל) למנהל המערכת

**מקורות:** `archive/.../TT2_TICKER_STATUS_MARKET_DATA_LOADING_SSOT.md`; `ui/src/utils/statusValues.js`, `statusAdapter.js`; `api/schemas/tickers.py` (TICKER_STATUS_VALUES).

---

### 9 — אלמנט סיכום מידע

**בעיה:** אלמנט סיכום מציג נתונים לא תואמים לסטטוסים בפועל של הטיקרים.

**נדרש:**
- בדיקה מדוע אי־ההתאמה (Backend: `active_tickers` = `is_active==True`; טבלה: `status` או fallback `is_active` — יישור לפי אפיון)
- בחינת נתונים סטטיסטיים נוספים רלוונטיים לסיכום (למשל: ממתין / פתוח / סגור / מבוטל; EOD vs Intraday)

**פריסה:** `tickersTableInit.js` — `updateSummary(summary)`; `tickers.html` — `totalTickers`, `activeTickers`; API `GET /tickers/summary` — `total_tickers`, `active_tickers`.

---

## 3א) דרישות עמוד ניהול מערכת (ממצאים 10–14)

**עמוד:** `http://localhost:8080/system_management.html`  
**קבצים:** `ui/src/views/management/systemManagement/system_management.html`, `systemManagementSettingsInit.js`, `systemManagementBackgroundJobsInit.js`

### 10 — תרחישי אוטומציה / Runtime בממשק

**דרישה:** את כל תרחישי "תרחישי אוטומציה / Runtime" יש לוודא שניתן **לאמת ולבצע** בממשק המשתמש בעמוד ניהול המערכת.

**נדרש:** מיפוי תרחישי AUTO-WP003 (ועוד) לעמוד system_management — כפתורים/קישורים/אינדיקציות שמאפשרים למנהל לאמת ולהריץ את התרחישים ישירות מהדף.

---

### 11 — היסטוריית ריצות

**דרישה:** לכל תהליך — אפשרות לצפייה בהיסטוריית ריצות, **או** ע"י לוג **או** ע"י מודל פרטים לכל רשומה.

**נדרש:** לכל משימה/תהליך — קישור או כפתור "היסטוריה" שפותח: (א) לוג ריצות, או (ב) רשימת ריצות עם מודל פרטים לכל שורה.

---

### 12 — פריט פעולות לכל משימה

**דרישה:** לכל משימה חייב להיות **פריט פעולות** הכולל:
- כפתור **הרצה מיידית**
- **הפעלה/עצירה** של הטימר (scheduler)
- **מודל עריכה** לשינוי הגדרות וליבלים (labels)

**נדרש:** עדכון `systemManagementBackgroundJobsInit.js` — כל שורת משימה תכלול תפריט/כפתורים: הרצה, טימר, עריכה. מודל עריכה עבור הגדרות משימה ותוויות.

---

### 13 — קונטיינר Market Data — וולידציה והתאמה

**דרישה:**
1. לוודא שהמערכת **אכן פועלת** בהתאם להגדרות בעמוד
2. המלצות וגבולות — **מדויקים** ועוברים **ולידציה לפני שמירה**
3. **משוב מפורט** למנהל האתר בעת שמירה (שגיאות, אזהרות, אישור הצלחה)

**נדרש:** `systemManagementSettingsInit.js` — ולידציה client-side לפי min/max ו-SSOT; Backend — ולידציה מלאה, החזרת `validation_errors` מפורטים. הצגת המשוב בממשק.

---

### 14 — כרטיס סיכום Market Data

**דרישה:** להוסיף לקונטיינר Market Data **כרטיס סיכום** המציג למנהל המערכת:
- **רמת עומס צפויה** בהתאם להגדרות וכמות הטיקרים במערכת
- **מכוון חום** (heat indicator) — ויזואליזציה לרמת העומס

**נדרש:** כרטיס חדש בתוך `tt-section` של Market Data — חישוב עומס (למשל: max_active_tickers, intraday_interval, כמות טיקרים פעילים) + מכוון חום (ירוק/צהוב/אדום או scale).

---

## 4) סטטוס

**BLOCK** — ממתין לסעיפים נוספים מ-Nimrod לפני מסמך סופי.  
מסמך זה — **טיוטה לשיתוף** עם הצוותים לצורך בחינה והכנה.

---

## 5) בחינה לעומק — סעיף 2.5

**שאלה:** איך יכול להיות שסעיפים 1+2 (מקור/עודכן ב מציגים "-"; רמזור אדום לכולם) לא נתגלו בבדיקות האוטומטיות?

### 5.1 מה האוטומציה באמת בודקת

| Check | מה נבדק | סוג אימות |
|-------|---------|-----------|
| **AUTO-WP003-01** | "Tickers table — current_price, last_close_price, price_source, price_as_of_utc, currency" | **סטטי (קוד)** |
| **AUTO-WP003-02** | "UI consistency: Tickers vs My Tickers (source/as-of/current-vs-last-close)" | **סטטי (קוד)** |

**עדות מ־TEAM_50_G7_PREHUMAN_UI_AUTOMATION_REPORT:**
> `tickersTableInit.js`: priceCell (...), lastCloseCell, **sourceCell (getPriceSourceLabel)**, **asOfCell (formatPriceAsOf)**. **All 5 fields bound per row.**

כלומר: האוטומציה מאמתת ש־**ה־binding קיים בקוד** (הקוד קורא ל־`getPriceSourceLabel`, `formatPriceAsOf` ויוצר תאים).  
**אין** אימות ש־**בהריצה** ה־API מחזיר נתונים ושהתאים מציגים ערך שונה מ־"—".

### 5.2 מה חסר — Runtime assertions

אין בדיקה אוטומטית (Selenium / Playwright / MCP script) שמבצעת:

1. ניווט ל־`/tickers`
2. assert: עבור ≥1 טיקר — עמודת "מקור" ≠ "—"
3. assert: עבור ≥1 טיקר — עמודת "עודכן ב" ≠ "—"
4. assert: לא כל הרמזורים אדומים (או: לפחות אחד ירוק/צהוב)

כלומר: **מבחינת אוטומציה — אין assertion על ערכים בזמן ריצה.**

### 5.3 מה דווח ב־R2 (לפני Pre-Human)

ב־**TEAM_50_GATE7_R2_QA_REPORT** (לפני Pre-Human):

| ממצא | תוצאה | עדות |
|------|--------|------|
| **1.1** מקור + עודכן ב | **PARTIAL** | "Binding exists; when API has value — displays. **When null → '—'**." |
| **1.2** רמזור | **BLOCK** | "AAPL, QQQ, SPY: **price_source=null → red**. Need EOD sync." |
| MCP | צילום מסך | "Tickers AAPL, AMZN, ANAU.MI, BTC-USD; **all $; red traffic lights**" |

כלומר: **ב־R2 הממצאים כן נתגלו** — אבל ב־**בדיקה אנושית** (MCP session + API checks), לא כחלק מ־**regression automation**.

### 5.4 הפער המרכזי

| שלב | אופן בדיקה | האם תפס 1+2? |
|-----|-------------|---------------|
| R2 QA (אנושי + MCP) | ידני / MCP | **כן** — 1.1 PARTIAL, 1.2 BLOCK |
| AUTO-WP003-01/02 (Pre-Human) | סטטי — code inspection | **לא** — בודק רק binding, לא runtime |
| GATE_7 Human (Nimrod) | ידני בדפדפן | **כן** — תפס את הסעיפים 1+2 |

ההבחנה: **AUTO-WP003-01/02** מתייחסים ל־"השדות קיימים וקושרים נכון" ולא ל־"הנתונים בפועל מוצגים".

### 5.5 סיבת השורש

1. **הגדרה שונה של "PASS"** — קוד עם binding נחשב PASS גם אם ה־API מחזיר null והתאים מציגים "—".
2. **חוסר E2E runtime assertions** — אין סקריפט אוטומטי שתריץ שווה־ערך ל־S7-WP003-01 ("3/3 sampled symbols show full transparency set").
3. **תרחישי Human (S7-WP003-01)** מגדירים במפורש: *"Expected: All fields are visible with values (not hidden, not `-` where value exists)"* — אך הם מיועדים לביצוע **ידני**, לא לאוטומציה.

### 5.6 המלצות לתיקון

| פעולה | בעלים |
|-------|--------|
| הוספת E2E assertion: עבור ≥3 סמלים — מקור ≠ "—" **וגם** עודכן ב ≠ "—" | Team 50 |
| הוספת assertion: לא כל הרמזורים אדומים (או: לפחות X ירוק/צהוב) | Team 50 |
| עדכון הגדרת AUTO-WP003-01: "binding **ובנוסף** runtime data presence" | Team 90 |
| הכללת S7-WP003-01 equivalents באוטומציה (Pre-Human gate) | Team 90, 50 |

---

**log_entry | TEAM_10 | NIMROD_G7_PRELIMINARY_FEEDBACK | BLOCK | 2025-01-30**
