# Team 90 -> Nimrod | GATE_7 Human Approval Scenarios — S002-P003-WP002
**project_domain:** TIKTRACK

**id:** TEAM_90_TO_NIMROD_S002_P003_WP002_GATE7_HUMAN_APPROVAL_SCENARIOS
**from:** Team 90 (External Validation Unit)
**to:** Nimrod (Human Approver)
**cc:** Team 10, Team 100, Team 170, Team 00, Team 190
**date:** 2026-03-01
**status:** READY_FOR_HUMAN_APPROVAL
**gate_id:** GATE_7
**work_package_id:** S002-P003-WP002

---

## Mandatory identity header

| Field | Value |
|---|---|
| roadmap_id | TIKTRACK_ROADMAP_LOCKED |
| stage_id | S002 |
| program_id | S002-P003 |
| work_package_id | S002-P003-WP002 |
| task_id | N/A |
| gate_id | GATE_7 |
| phase_owner | Team 90 |
| required_ssm_version | 1.0.0 |
| required_active_stage | S002 |
| project_domain | TIKTRACK |

---

## Approval objective

אישור אנושי על חוויית שימוש ותהליכי UI עבור D22 / D34 / D35, לאחר ש-GATE_6 אושר.  
בדיקה זו היא **בדיקת דפדפן בלבד**: ממשקים, טפסים, הודעות שגיאה, רענון טבלאות וזרימת משתמש.

---

## כללי בדיקה מחייבים

1. אין צורך לבדוק טרמינל, לוגים, קבצי `/tmp`, exit codes או סקריפטים.
2. יש לבדוק דרך הממשק בלבד: פתיחת עמודים, לחיצה על כפתורים, הזנה בטפסים, שמירה, עריכה, מחיקה, סינון.
3. אם חסר נתון לצורך הבדיקה, מותר ליצור אותו דרך ה-UI כחלק מהתרחיש.
4. מטרת הבדיקה היא לכסות דרך ה-UI את הזרימות שמממשות את ה-API בפועל: list, create, update, delete, filters, negative validation.

---

## מיפוי כיסוי פונקציונלי (UI -> API בפועל)

| אזור | כיסוי פונקציונלי שנדרש להיבדק דרך UI |
|---|---|
| D22 — Tickers | טעינה, סיכום, יצירה, עריכה, מחיקה, סינון לפי סוג, סינון לפי פעיל/לא פעיל |
| D34 — Alerts | טעינה, סיכום, יצירה, עריכה, toggle פעיל/לא פעיל, מחיקה, סינון לפי סוג ישות, ולידציית שדות |
| D35 — Notes | טעינה, צפייה, יצירה, עריכה, מחיקה, Rich Text, העלאת קבצים, הסרת קבצים, ולידציות תוכן/קבצים |

---

## תרחיש 1 — D22 ניהול טיקרים (מסך דפדפן)

**מטרה:** לאשר שעמוד הטיקרים עובד מקצה לקצה דרך UI, כולל CRUD וסינונים.

### שלבי בדיקה

1. פתח את עמוד **ניהול טיקרים** (`D22`).
2. לחץ **הוספת טיקר**.
3. צור טיקר חדש עם נתון שקל לזהות, לדוגמה:
   - `symbol`: `G7T90`
   - `company_name`: `Gate 7 Test Ticker`
   - `ticker_type`: `STOCK`
   - `status`: `active`
4. שמור.
5. אמת שהשורה החדשה מופיעה בטבלה ושמונה:
   - הסמל מופיע
   - סוג = `STOCK`
   - סטטוס מוצג כפעיל
6. הפעל סינון לפי **סוג = STOCK** ואמת שהטיקר נשאר גלוי.
7. הפעל סינון לפי **פעילים בלבד** ואמת שהטיקר נשאר גלוי.
8. ערוך את אותו טיקר ושנה סטטוס ל-`cancelled`.
9. חזור לסינון **פעילים בלבד** ואמת שהטיקר לא מופיע.
10. חזור ל"תצוגת הכל" או "לא פעילים" ואמת שהטיקר כן מופיע.
11. מחק את הטיקר שיצרת ואמת שהוא נעלם מהטבלה.

### מקרי קצה חובה

- פתח שוב **הוספת טיקר**, נסה לשמור ללא `symbol`:
  - צפוי: הממשק חוסם שמירה / מציג דרישת שדה חובה.
- הזן `symbol` באותיות קטנות (למשל `g7t90`) ושמור:
  - צפוי: הערך נשמר באותיות גדולות, או לפחות מוצג בעקביות תקינה בטבלה.

### PASS

- כל שלבי היצירה, העריכה, הסינון והמחיקה עובדים.
- אין תקיעה, אין רענון שבור, אין מצב שבו הטבלה/מונה נשארים במצב לא מעודכן.

---

## תרחיש 2 — D34 התראות (מסך דפדפן)

**מטרה:** לאשר שעמוד ההתראות תומך בזרימת משתמש מלאה: create, edit, toggle, filter, delete.

### שלבי בדיקה

1. פתח את עמוד **התראות** (`D34`).
2. לחץ **הוספת התראה**.
3. צור התראה חדשה עם נתון שקל לזהות, לדוגמה:
   - `title`: `G7 Alert Test`
   - `target_type`: `ticker`
   - `alert_type`: `PRICE`
   - `message`: `Human approval flow check`
4. שמור.
5. אמת שההתראה מופיעה בטבלה.
6. אמת שהמונה/סיכום בעמוד מתעדכנים בצורה סבירה (אין סטטוס שבור, אין טבלה ריקה לא תקינה).
7. הפעל סינון לפי סוג ישות **טיקר** ואמת שההתראה נשארת גלויה.
8. ערוך את ההתראה:
   - שנה כותרת ל-`G7 Alert Test Edited`
   - שנה את ההודעה
9. שמור ואמת שהשינויים מופיעים בטבלה/בפרטים.
10. הפעל את פעולת **toggle active** (פעיל/לא פעיל) משורת הפעולות.
11. אמת שהסטטוס השתנה בטבלה.
12. מחק את ההתראה שיצרת ואמת שהיא נעלמה מהטבלה.

### מקרי קצה חובה

- נסה ליצור התראה בלי כותרת:
  - צפוי: אי אפשר לשמור; מתקבלת הודעת חובה להזין כותרת.
- הזן כותרת ארוכה אך חוקית ועדכן:
  - צפוי: נשמר ללא שבירת layout או קיטוע לא תקין של השורה.
- אם קיימת אפשרות צפייה בפרטי שורה:
  - פתח "פרטים" ואמת שהנתונים תואמים לכותרת/סוג/סטטוס.

### PASS

- יצירה, עריכה, שינוי סטטוס, סינון ומחיקה עובדים.
- ולידציית כותרת עובדת.
- אין מצב שבו שורה נשארת "תקועה" עם סטטוס ישן אחרי פעולה.

---

## תרחיש 3 — D35 הערות (מסך דפדפן)

**מטרה:** לאשר שעמוד ההערות עובד עם Rich Text וקבצים, כולל ולידציות ושמירה עקבית.

### שלבי בדיקה

1. פתח את עמוד **הערות** (`D35`).
2. לחץ **הוספת הערה**.
3. צור הערה חדשה:
   - `parent_type`: `general`
   - `title`: אפשר להשאיר ריק כדי לבדוק גזירת כותרת אוטומטית, או להזין `G7 Note Test`
   - `content`: הזן תוכן Rich Text ברור, לדוגמה:
     - שורה מודגשת
     - רשימה קצרה
     - טקסט רגיל עם ביטוי מזהה כמו `G7 note body`
4. שמור.
5. אמת שההערה מופיעה בטבלה.
6. פתח **עריכה** לאותה הערה.
7. עדכן את תוכן ה-Rich Text ואמת שלאחר שמירה התצוגה בטבלה/בפרטים משקפת את השינוי.
8. אם יש לך קובץ מקומי קטן חוקי (עד 1MB), צרף לפחות קובץ אחד מותר:
   - תמונה / PDF / Excel / Word
9. שמור ואמת שמוצג חיווי לקובץ המצורף.
10. פתח שוב עריכה, הסר את הקובץ, שמור, ואמת שהקובץ הוסר.
11. מחק את ההערה שיצרת ואמת שהיא נעלמה מהטבלה.

### מקרי קצה חובה

- נסה לשמור הערה עם **תוכן ריק**:
  - צפוי: מתקבלת הודעת `תוכן חובה` או חסימה מקבילה; אין שמירה.
- נסה לצרף **4 קבצים**:
  - צפוי: הממשק חוסם את הקובץ הרביעי / מציג הודעה על מכסה מלאה.
- נסה לצרף **קובץ גדול מ-1MB**:
  - צפוי: מתקבלת הודעת שגיאה מתאימה; אין קבלה שקטה של קובץ לא חוקי.
- נסה לצרף **קובץ לא נתמך** (לדוגמה `.txt`):
  - צפוי: בחירה חסומה או מתקבלת שגיאה ברורה; אין שמירה תקינה של קובץ לא מותר.
- בדיקת XSS בסיסית:
  - צור/ערוך הערה עם טקסט כגון `<script>alert(1)</script>` כחלק מהתוכן.
  - צפוי: לא קופץ alert בדפדפן; התוכן מוצג באופן בטוח (כטקסט או בצורה מסוננת), ללא הרצת קוד.

### PASS

- Rich Text נשמר ונערך.
- ולידציות תוכן וקבצים עובדות.
- מגבלת 3 קבצים ומגבלת 1MB נאכפות.
- אין הרצת קוד לא בטוח בתצוגה.

---

## כלל החלטה

- **אישור**: שלושת התרחישים עוברים ללא ממצא חוסם.
- **פסילה**: כל כשל מהותי באחד מהתרחישים (שמירה שבורה, CRUD לא עובד, ולידציה חסרה, קובץ לא נאכף, XSS, סטטוס/טבלה לא מתעדכנים, UI לא עקבי).

---

## פורמט תשובה מבוקש ממך

נא להחזיר תשובה בעברית בלבד:

- אם הכל תקין: `אישור`
- אם יש כשל: `פסילה`

אם יש `פסילה`, נא לצרף סעיפים ממוספרים כך:

1. שם העמוד (`D22` / `D34` / `D35`)
2. הפעולה שבוצעה
3. מה ציפית לראות
4. מה קרה בפועל
5. האם זה חוסם (`חוסם` / `לא חוסם`)

דוגמה קצרה:

```text
פסילה
1. D34 - בעריכת התראה, לחיצה על שמירה לא עדכנה את הכותרת בטבלה.
   ציפיתי: הכותרת החדשה תופיע מיד.
   בפועל: נשארה הכותרת הישנה עד רענון ידני.
   סיווג: חוסם.
```

---

**log_entry | TEAM_90 | S002_P003_WP002 | GATE_7_BROWSER_SCENARIOS_ISSUED_TO_HUMAN_APPROVER | 2026-03-01**
