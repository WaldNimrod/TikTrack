# Team 90 -> Nimrod | GATE_7 Human Approval Scenarios (Option C) — S002-P003-WP002
**project_domain:** TIKTRACK

**id:** TEAM_90_TO_NIMROD_S002_P003_WP002_GATE7_HUMAN_APPROVAL_SCENARIOS_v1.4.0  
**from:** Team 90 (External Validation Unit — GATE_7 owner)  
**to:** Nimrod (Human Approval Authority)  
**cc:** Team 10, Team 00, Team 100, Team 50  
**date:** 2026-03-06  
**status:** ACTIVE_FOR_HUMAN_SIGNOFF  
**gate_id:** GATE_7  
**work_package_id:** S002-P003-WP002  
**approval_mode:** Option C (Browser Full Walkthrough)

---

## Mandatory identity header

| Field | Value |
|---|---|
| roadmap_id | PHOENIX_ROADMAP |
| stage_id | S002 |
| program_id | S002-P003 |
| work_package_id | S002-P003-WP002 |
| task_id | N/A |
| gate_id | GATE_7 |
| phase_owner | Team 90 |
| required_ssm_version | 1.0.0 |
| required_active_stage | S002 |
| project_domain | TIKTRACK |

---

## כללי ביצוע מחייבים

1. בדיקות בדפדפן בלבד (ללא טרמינל/לוגים/קבצי מערכת).
2. לבצע תרחישים על D22 + D33 + D34 + D35 + השפעות משימות רקע דרך ה-UI.
3. פלט החלטה בעברית בלבד: `אישור` או `פסילה` + סעיפים ממוספרים.

---

## תרחיש 1 — D22 ניהול טיקרים

1. הוספת טיקר חדש עם סימבול תקין -> רשומה נוצרת ומוצגת.
2. ניסיון הוספה עם סימבול לא תקין -> שגיאת ולידציה ברורה, ללא יצירה.
3. ניסיון הוספה עם סימבול כפול -> חסימה.
4. שינוי סטטוס טיקר -> נשמר ומופיע מייד בטבלה.
5. מחיקת טיקר עם תלותים -> התנהגות תקינה (חסימה/אישור עם הודעה ברורה).

---

## תרחיש 2 — D33 הטיקרים שלי

1. הוספה מרשימת טיקרים קיימת (dropdown) -> שיוך למשתמש נוצר.
2. מעבר למסלול "הוספת טיקר למערכת" -> UI ברור ומאפס בחירה קודמת.
3. טיקר חדש ללא נתוני שוק -> חסימה/אזהרה אדומה כנדרש.
4. עריכה/מחיקה של רשומת "הטיקרים שלי" -> נשמר ומתרענן מייד בטבלה.

---

## תרחיש 3 — D34 התראות

1. יצירת התראה עם קישור ספציפי + תנאי מלא (field/operator/value) -> שמירה תקינה.
2. ניסיון שמירה בלי תנאי תקין -> חסימה.
3. ניסיון שמירה עם קישור `general` -> חסימה.
4. עריכת התראה (כולל linked entity כאשר מותר) -> נשמר בפועל.
5. פילטרים פנימיים בעמוד עובדים (כולל "הכול").
6. סטטוסים מרונדרים כבאג'ים ובצבעי ישות.

---

## תרחיש 4 — D35 הערות

1. יצירת הערה עם קישור ספציפי + תוכן rich text -> שמירה תקינה.
2. ניסיון יצירה ללא קישור ישות/תאריך-שעה -> חסימה.
3. העלאת קובץ תקין -> מופיע מייד במודול ובטבלה.
4. שגיאת קובץ (סוג/גודל) -> שגיאה מוצגת בתוך המודול, המודול לא נסגר.
5. פתיחה/הורדה/הסרה של קובץ מצורף -> מתעדכן מייד בטבלה ובפרטים.

---

## תרחיש 5 — Background Tasks (דרך ה-UI בלבד)

1. לוודא שבמסכים שמציגים "מקור נתון/עדכון אחרון" אין מצב מטעה של נתון מיושן.
2. לוודא שהצגת מקור מחיר (EOD מול intraday fallback) עקבית במסכי טיקרים/הטיקרים שלי.
3. לוודא שלא נוצרות כפילויות רשומות כתוצאה מפעולות רקע נראות למשתמש.

---

## פורמט תשובה נדרש

אם תקין:

```text
אישור
```

אם לא תקין:

```text
פסילה
1. <עמוד> - <פעולה> - <מה ציפית> - <מה קרה בפועל> - <חוסם/לא חוסם>
2. ...
```

---

**log_entry | TEAM_90 | TO_NIMROD | S002_P003_WP002_GATE7_HUMAN_APPROVAL_SCENARIOS_v1_4_0 | ACTIVE | 2026-03-06**
