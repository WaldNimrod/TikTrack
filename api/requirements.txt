# TikTrack Project Phoenix - Backend Dependencies
# Team 20 (Backend) - Session 01

# FastAPI Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Authentication & Security (KB-010: PyJWT replaces python-jose — no ecdsa dependency)
PyJWT>=2.8.0  # KB-010: Replaces python-jose (no ecdsa dependency)
bcrypt>=5.0.0  # Direct bcrypt usage (replaced passlib)
cryptography>=41.0.0
slowapi>=0.1.9  # Rate limiting for FastAPI

# Validation
pydantic>=2.5.0

# Rich-Text Sanitization (SOP-012) — text-align support
bleach[css]>=6.0.0
pydantic-settings>=2.1.0
email-validator>=2.0.0  # Required for Pydantic EmailStr validation

# Background tasks (ARCHITECT_DIRECTIVE_BACKGROUND_TASK_ORCHESTRATION)
# Required for FastAPI lifespan — backend startup fails without it
apscheduler>=3.10.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
ulid-py>=1.1.0  # ULID support for UUID→ULID conversion

# External Market Data — P3-008, P3-009 (EXTERNAL_PROVIDER_*_SPEC)
yfinance>=0.2.36

# Testing (development)
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
pre-commit>=3.7.0
