# 📋 מידע משלים נדרש — סגירת משימות ב-100% (ללא ניחושים)

**id:** `TT2_PHASE_2_CLOSURE_TASK_SPEC_SUPPLEMENT_REQUEST`  
**owner:** Team 10 (The Gateway)  
**purpose:** רשימת פריטי מידע שהאדריכלית/צוותים צריכים לספק כדי שכל משימה בתוכנית הסגירה תהיה **סגורה וברורה ב-100%** — ללא מקום לניחושים או יצירתיות יתר.  
**last_updated:** 2026-02-09

---

## 🎯 עקרון

כל משימה חייבת לכלול: **מה בדיוק לעשות**, **איפה (נתיב/קבצים)**, **איך לאמת**, ו**מקור אמת יחיד (SSOT)** לפרטים. אם חסר אחד מאלה — המשימה לא "סגורה".

---

## 1. Team 10 (השער)

| משימה | מה חסר לסגירה 100% |
|--------|----------------------|
| **1.1.2 אכיפת SLA 30/40** | **איך לאכוף:** checklist ב-review? שער לפני merge? רשימת קבצים/תיקיות ששייכים ל-40 (Presentational) vs 30 (Containers). **קריטריון מעבר:** איך Team 10 יודע ש"הושלם"? (למשל: "אין קובץ ב-`ui/src/views/financial/**` שמכיל קריאות API ישירות" — אם זו ההגדרה). |
| **1.1.3 וידוא make db-test-clean** | **הגדרה מדויקת:** מה בדיוק הפקודה `make db-test-clean` אמורה לעשות (אילו טבלאות/שורות נמחקות)? **קריטריון הצלחה:** פלט מצופה (exit code 0? הודעת "DB sterile"?) ואיך Team 60 מוכיח שזה עובד. **מיקום:** איפה ה-Makefile וה-target (נתיב מלא). |

---

## 2. Team 20 / Team 60 (Backend & Infra)

| משימה | מה חסר לסגירה 100% |
|--------|----------------------|
| **1.2.1 Endpoints Summary ו-Conversions (Option A)** | **רשימת Endpoints סגורה:** למשל: `GET /api/v1/brokers_fees/summary`, `GET /api/v1/cash_flows/currency_conversions` (או אחר). **חוזה:** קישור ל-OpenAPI/SSOT עם request/response schema, query params, status codes. **קריטריון מעבר:** אילו בדיקות (manual/automated) מאשרות "API פעיל". |
| **1.2.2 נעילת פורטים 8080/8082 + Precision 20,6** | **פורטים:** אילו קבצי config משמשים לאימות (vite.config, backend CORS, וכו') — רשימת קבצים. **Precision 20,6:** באילו טבלאות ובאילו עמודות בדיוק (שם טבלה, שם עמודה, סוג NUMERIC(20,6)). מסמך SSOT או רשימה ממוספרת. |
| **1.2.3 Python Seeders + is_test_data** | **רשימת ישויות:** אילו מודלים/טבלאות נזרעים (trading_accounts, brokers_fees, cash_flows, users, וכו'). **מבנה הפלאג:** שם השדה המדויק (`is_test_data`?), טיפוס (boolean?), איפה מופיע ב-DB. **מיקום:** נתיב לתיקיית הסקריפטים ולתצורת Makefile. **הגדרת "DB סטרילי":** אחרי `make db-test-clean` — אילו טבלאות ריקות / אילו שורות נמחקות (רק שורות עם is_test_data=true?). |

---

## 3. Team 30 / Team 40 (Frontend)

| משימה | מה חסר לסגירה 100% |
|--------|----------------------|
| **1.3.1 Retrofit רספונסיביות (Option D)** | **מפרט Option D:** אילו עמודות "Sticky Start" (מזהה?) ואילו "Sticky End" (פעולות?) — לפי עמוד (D16/D18/D21) ו-id של הטבלה. **נוסחת clamp():** דוגמה מדויקת (למשל `clamp(80px, 15%, 200px)`) או טווחים מוגדרים לכל עמודה. **איסור display:none:** באילו הקשרים (רק עמודות בטבלאות? גם ב-mobile menu?). **קבצי CSS/מבנה:** אילו קבצים לעדכן (נתיבים), ואיזה קומפוננט/class אחראי לכל טבלה. |
| **1.3.2 console.log → audit.maskedLog** | **הבהרת שם:** בפרויקט קיימים `maskedLog.js` (maskedLog, maskedLogWithTimestamp) ו-`audit.js` (audit.log, audit.error). מה **בדיוק** החובה: `audit.maskedLog` (אם קיים) או `maskedLog` / `maskedLogWithTimestamp` מ-`maskedLog.js`? **Scope:** אילו תיקיות/סיומות (רק `ui/src`? כולל tests? כולל קבצי config?). **כלל וולידציה:** "אפס מופעים של console.log בקבצים X" — רשימת globs או תיקיות. **חריגים:** האם מותר console.log בתוך `if (DEBUG_MODE)` או רק maskedLog/audit? |
| **1.3.3 הקשחת טרנספורמרים (NaN/Undefined)** | **רשימת שדות כספיים:** לפי עמוד (D16/D18/D21) — אילו שדות ב-response חייבים להפוך למספר (או null) ולא להשאיר NaN/undefined. **התנהגות מחייבת:** ערך null vs 0 כשחסר; פורמט מספר (עיגול? precision?). **מיקום:** אילו פונקציות ב-`transformers.js` (או קובץ אחר) מטפלות באילו שדות — מיפוי מפורש (שם פונקציה ↔ שדות). **קריטריון מעבר:** איך מאמתים (E2E בודק שאין "NaN" ב-DOM? בדיקת unit ל-transformers?). |

---

## 4. כללי (לכל הצוותים)

| נושא | מה חסר |
|------|----------|
| **דדליינים** | תאריך יעד לכל שלב (או לכל משימה) — נדרש להנעת ביצוע וללא דחיות לא מוגדרות. |
| **סדר ביצוע** | האם יש תלות: למשל "1.2.1 לפני 1.3.3" (Frontend צריך API כדי לבדוק טרנספורמרים)? רשימת תלויות מפורשת. |
| **אישור ומעבר** | מי מאשר "משימה הושלמה" — Team 10? Team 90? G-Lead? לכל משימה — בעל האישור. |

---

## 5. המלצה למילוי

1. **האדריכלית:** למלא את הסעיפים שקשורים להחלטות (Option D, Option A, Precision, SLA אכיפה, לוגים).
2. **Team 10:** לאחר קבלת המילוי — להטמיע את הפריטים **בתוך** תוכנית הסגירה (`TT2_PHASE_2_CLOSURE_WORK_PLAN.md`) או במסמכי SSOT נפרדים (למשל ספציפיקציה ל-Option D, ספציפיקציה ל-Endpoints), ולוודא שכל משימה מפנה למסמך/רשימה הסגורה.
3. **צוות 90:** לאשר שהמפרט הממולא מספיק לולידציה (Gate B) ללא פרשנות.

---

**כשכל הסעיפים ממולאים ומשולבים בתוכנית — המשימות סגורות וברורות ב-100%.**
**log_entry | Team 10 | TASK_SPEC_SUPPLEMENT_REQUEST | CREATED | 2026-02-09**
