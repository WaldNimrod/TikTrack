# Info Summary Configurations - תצורות מערכת הסיכומים

> **Important (Nov 2025):** כל קונפיגורציה חייבת לכלול `tableType` תואם לשם שבו הטבלה נרשמת ב-`UnifiedTableSystem` / `TableDataRegistry`. ללא שדה זה המערכת תיפול לנתוני fallback ולא תבסס את הסטטיסטיקות על הנתונים המסוננים.
