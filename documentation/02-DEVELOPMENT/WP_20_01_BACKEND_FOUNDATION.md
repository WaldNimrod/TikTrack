# 🔧 משימה 20.1: שלד השרת וקובית המשתמשים

**יעד:** הקמת ה-Walking Skeleton בפורט 8080.

### צעדים לביצוע:
- [ ] הקמת סביבת FastAPI ותשתית Middleware (CORS, Auth).
- [ ] מימוש מחלקת בסיס `BaseBusinessService` עם Logger.
- [ ] הקמת קובית `Foundation_Users` (Layer 1).
- [ ] הוכחת קריאת Auth Token ממערכת הלגסי.