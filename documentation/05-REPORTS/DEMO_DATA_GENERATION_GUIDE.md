# מדריך שימוש: יצירת נתוני דוגמה
## Demo Data Generation Guide

**תאריך יצירה:** 29 בינואר 2025  
**גרסה:** 1.0.0  
**מטרה:** מדריך מפורט ליצירת נתוני דוגמה מלאים למערכת TikTrack

---

## 📋 תוכן עניינים

1. [מטרת הסקריפט](#מטרת-הסקריפט)
2. [דרישות מקדימות](#דרישות-מקדימות)
3. [הוראות הרצה](#הוראות-הרצה)
4. [מה הסקריפט יוצר](#מה-הסקריפט-יוצר)
5. [פרמטרים אפשריים](#פרמטרים-אפשריים)
6. [טיפול בשגיאות](#טיפול-בשגיאות)
7. [דוגמאות שימוש](#דוגמאות-שימוש)
8. [פירוט טכני](#פירוט-טכני)

---

## 🎯 מטרת הסקריפט

הסקריפט `generate_demo_data.py` נועד ליצור נתוני דוגמה מלאים למערכת TikTrack לאחר ניקוי נתוני המשתמש (פייז 1).

### למה צריך את זה?
- **הדגמות ופרזנטציות** - להצגת המערכת עם נתונים ריאליסטיים
- **בדיקות UI** - לבדיקת ממשק המשתמש עם נתונים מגוונים
- **פיתוח ובדיקות** - לפיתוח פיצ'רים חדשים מול נתונים אמיתיים
- **הדרכה** - להדרכת משתמשים חדשים

### מה הסקריפט עושה?
- בודק את מבנה בסיס הנתונים לפני יצירה
- יוצר נתונים ריאליסטיים עם קשרים נכונים
- מפיץ תאריכים לפי הדרישות (40% חצי שנה, 70% מהם 3 חודשים)
- מבטיח קשרים לוגיים בין כל הישויות

---

## ✅ דרישות מקדימות

לפני הרצת הסקריפט, יש לוודא:

1. **שלב 1 הושלם בהצלחה**
   - ניקוי נתוני המשתמש הושלם
   - בסיס הנתונים נקי (רק SPY, פרופיל פעיל אחד)

2. **בסיס הנתונים תקין**
   - טבלאות קיימות ומעודכנות
   - Foreign Keys תקינים
   - נתוני בסיס קיימים (מטבעות, משתמש פעיל)

3. **סביבת עבודה**
   - Python 3.7+
   - כל החבילות הנדרשות מותקנות
   - גישה לבסיס הנתונים

4. **בדיקה ידנית הושלמה**
   - שלב 10.5 במסמך העבודה - בדיקה ידנית בדפדפן
   - אישור שהמערכת פועלת תקין

---

## 🚀 הוראות הרצה

### שיטה 1: הרצה ישירה (מומלץ)

```bash
cd Backend
python3 scripts/generate_demo_data.py
```

### שיטה 2: עם פרמטרים

```bash
# Dry run - רק אימות ללא יצירת נתונים
python3 Backend/scripts/generate_demo_data.py --dry-run

# עם הודעות מפורטות
python3 Backend/scripts/generate_demo_data.py --verbose
```

### שיטה 3: מהתיקייה הראשית

```bash
python3 Backend/scripts/generate_demo_data.py
```

---

## 📊 מה הסקריפט יוצר

הסקריפט יוצר נתוני דוגמה מלאים לפי הקונפיגורציה הבאה:

### טיקרים (50)
- **90% מטבע USD** - טיקרים אמריקאיים (AAPL, MSFT, GOOGL, וכו')
- **10% מטבעות אחרים** - ILS או EUR
- **מגוון סוגים:** stocks, ETFs, crypto
- **שמות ריאליסטיים** מתוך רשימת דוגמאות

### חשבונות מסחר (3)

#### חשבון 1: חשבון ראשי
- **70% מהפעילות** במערכת
- **כל הסווינג** (100% מהתוכניות מסוג swing)
- מטבע: USD
- יתרה התחלתית: $100,000

#### חשבון 2: חשבון פנסיוני
- השקעות ארוכות טווח (investment, passive)
- מטבע: USD
- יתרה התחלתית: $500,000

#### חשבון 3: חשבון מטבע אחר
- השקעות ארוכות טווח
- מטבע: ILS או EUR
- יתרה התחלתית: 200,000 (במטבע הנבחר)

### תוכניות טרייד (120)
- **50% סווינג** - כל הסווינג בחשבון הראשי
- **50% השקעות ארוכות טווח** - בחשבונות האחרים
- **90% לונג, 10% שורט**
- **40% בחצי שנה האחרונה** (מתוכם 70% בשלושת החודשים האחרונים)
- **60% בשנה וחצי הקודמת**

### טריידים (80)
- **70% מתוכניות קיימות** - טריידים שנוצרו מתוכניות
- **30% עצמאיים** - טריידים ללא תוכנית
- **סטטוסים מגוונים:** open, closed, cancelled
- **קישור נכון** לחשבונות, טיקרים ותוכניות

### ביצועים (Executions)
- **לכל טרייד:** לפחות 2 ביצועים
  - ביצוע פתיחה (buy/short)
  - ביצוע סגירה (sell/cover) - אם הטרייד סגור
- **תאריכים לוגיים:** בין פתיחה לסגירה
- **מחירים ריאליסטיים** בהתאם ל-P/L

### תזרימי מזומן (Cash Flows)
- **הפקדות ראשוניות** לכל חשבון
- **הפקדות ומשיכות** מפוזרות על פני התקופה
- **דיבידנדים** מטריידים סגורים
- **עמלות** מפוזרות על פני התקופה

### התראות (Alerts)
- **התראות על טיקרים** (10)
- **התראות על טריידים** (15)
- **התראות על תוכניות** (10)
- **תאריכים רלוונטיים** ומגוונים

### הערות (Notes)
- **הערות על טיקרים** (8)
- **הערות על טריידים** (12)
- **הערות על תוכניות** (10)
- **הערות על חשבונות** (3)

---

## ⚙️ פרמטרים אפשריים

### `--dry-run`
**תיאור:** בודק את מבנה בסיס הנתונים בלבד, לא יוצר נתונים

**שימוש:**
```bash
python3 Backend/scripts/generate_demo_data.py --dry-run
```

**מתי להשתמש:**
- לפני הרצה אמיתית - לוודא שהכל תקין
- לבדיקת מבנה DB לפני עדכונים

### `--verbose`
**תיאור:** מציג הודעות מפורטות על כל שלב

**שימוש:**
```bash
python3 Backend/scripts/generate_demo_data.py --verbose
```

**מתי להשתמש:**
- כאשר רוצים לראות התקדמות מפורטת
- לניפוי באגים

---

## ❌ טיפול בשגיאות

הסקריפט כולל מערכת טיפול בשגיאות מתקדמת עם הודעות ברורות בעברית.

### סוגי שגיאות

#### 1. SchemaValidationError - שגיאת מבנה DB

**מתי קורה:**
- טבלה חסרה
- שדה חסר בטבלה
- בעיית Foreign Key

**דוגמה:**
```
❌ טבלה חסרה: 'tickers' לא נמצאה בבסיס הנתונים
📍 מיקום: בדיקת מבנה DB לפני יצירת נתוני דוגמה
💡 פתרון: ודא שהטבלה קיימת ורוץ את migration המתאים
```

**פתרון:**
- בדוק שהטבלה קיימת בבסיס הנתונים
- הרוץ migration מתאים אם חסר
- ודא שכל המודלים מעודכנים

#### 2. DataGenerationError - שגיאה ביצירת נתונים

**מתי קורה:**
- חסר נתון בסיס (משתמש, מטבע)
- שגיאה ביצירת ישות ספציפית

**דוגמה:**
```
❌ שגיאה ביצירת tickers: מטבע USD לא נמצא
📍 פרטים: USD currency not found in database
💡 פתרון: ודא שמטבע USD קיים במערכת
```

**פתרון:**
- ודא שכל נתוני הבסיס קיימים (משתמש פעיל, מטבע USD, טיקר SPY)
- בדוק את הקונפיגורציה
- ודא ששלב 1 הושלם בהצלחה

#### 3. שגיאות כלליות

**מתי קורה:**
- שגיאה לא צפויה
- בעיית חיבור ל-DB

**פתרון:**
- בדוק את חיבור בסיס הנתונים
- בדוק את הלוגים למסר שגיאה מלא
- ודא שהסביבה מוכנה

### התנהגות בשגיאה

- **Rollback אוטומטי:** כל השינויים מבוטלים
- **הודעה ברורה:** הסבר בעברית עם מיקום מדויק
- **הצעת פתרון:** מה לעשות כדי לתקן

---

## 💡 דוגמאות שימוש

### דוגמה 1: הרצה ראשונה

```bash
# 1. Dry run לבדיקה
python3 Backend/scripts/generate_demo_data.py --dry-run

# 2. אם הכל תקין - הרצה מלאה
python3 Backend/scripts/generate_demo_data.py
```

### דוגמה 2: בדיקה מפורטת

```bash
# הרצה עם הודעות מפורטות
python3 Backend/scripts/generate_demo_data.py --verbose
```

### דוגמה 3: אחרי עדכון מבנה DB

```bash
# 1. Dry run לבדיקת מבנה
python3 Backend/scripts/generate_demo_data.py --dry-run

# 2. אם יש שגיאה - תקן את המבנה

# 3. הרצה מלאה
python3 Backend/scripts/generate_demo_data.py
```

---

## 🔧 פירוט טכני

### סדר יצירת הנתונים

הסקריפט יוצר את הנתונים בסדר מסוים כדי לשמור על קשרים נכונים:

1. **טיקרים** - ללא תלויות
2. **חשבונות מסחר** - תלויים במטבעות
3. **תוכניות טרייד** - תלויים בחשבונות וטיקרים
4. **טריידים** - תלויים בחשבונות, טיקרים ותוכניות
5. **ביצועים** - תלויים בטריידים
6. **תזרימי מזומן** - תלויים בחשבונות וטריידים
7. **התראות** - תלויים בטיקרים, טריידים ותוכניות
8. **הערות** - תלויות בכל הישויות

### פיזור תאריכים

הסקריפט מפיץ תאריכים לפי הדרישות הבאות:

- **40% מהנתונים** בחצי שנה האחרונה (0-180 ימים)
- **מתוכם 70%** בשלושת החודשים האחרונים (0-90 ימים)
- **60% מהנתונים** בשנה וחצי הקודמת (180-730 ימים)
- **סך הכל:** כשנתיים אחורה ממועד ביצוע הסקריפט

### קונפיגורציה

הקונפיגורציה מוגדרת בקבוע `DEMO_CONFIG` בתחילת הקובץ:

```python
DEMO_CONFIG = {
    'trading_accounts': {
        'count': 3,
        'primary_account_activity_percent': 70,
        'primary_account_swing_only': True
    },
    'tickers': {
        'count': 50,
        'usd_percent': 90,
        'other_currencies': ['ILS', 'EUR']
    },
    'trade_plans': {
        'count': 120,
        'swing_percent': 50,
        'long_percent': 90
    },
    'trades': {
        'count': 80,
        'from_plans_percent': 70
    },
    'time_distribution': {
        'last_6_months_percent': 40,
        'last_3_months_of_6_percent': 70,
        'total_years_back': 2
    }
}
```

### מחלקות עיקריות

#### `DatabaseValidator`
- בודק את מבנה DB לפני יצירה
- אימות טבלאות, שדות ו-Foreign Keys
- החזרת שגיאות ברורות

#### `DateDistributionGenerator`
- מייצר תאריכים לפי הפיזור הנדרש
- פונקציות לסוגי פיזור שונים

#### `DataRelationshipManager`
- מנהל את הקשרים בין ישויות
- עוזר ביצירת קישורים נכונים

#### `DemoDataGenerator`
- המחלקה הראשית ליצירת נתונים
- מכילה פונקציות ליצירת כל סוג ישות

### שימוש ב-SQLAlchemy

הסקריפט משתמש ב-SQLAlchemy ORM:
- שימוש במודלים הקיימים (`models/`)
- שמירה על Foreign Keys אוטומטית
- Rollback אוטומטי בשגיאה

---

## 📝 הערות חשובות

### לפני הרצה
- ⚠️ **ודא ששלב 1 הושלם** - בסיס הנתונים נקי
- ⚠️ **בצע בדיקה ידנית** - שלב 10.5 במסמך העבודה
- ⚠️ **גבה את בסיס הנתונים** - למקרה שתרצה לחזור

### אחרי הרצה
- ✅ **בדוק את הנתונים** - פתח את המערכת בדפדפן
- ✅ **ודא שהקשרים תקינים** - טריידים מקושרים לתוכניות, וכו'
- ✅ **בדוק את התאריכים** - ודא שהם רלוונטיים ומופצים נכון

### בעיות נפוצות

**בעיה:** "מטבע USD לא נמצא"
- **פתרון:** ודא שמטבע USD קיים ב-`currencies` table

**בעיה:** "לא נמצא משתמש פעיל"
- **פתרון:** ודא שיש משתמש עם `is_active=True` ב-`users` table

**בעיה:** "טבלה חסרה"
- **פתרון:** הרוץ migration מתאים או בדוק את מבנה DB

---

## 🔗 קישורים

- **מסמך תהליך הניקוי:** [USER_DATA_CLEANUP_PROCESS.md](USER_DATA_CLEANUP_PROCESS.md)
- **מיקום הסקריפט:** `Backend/scripts/generate_demo_data.py`
- **מודלים:** `Backend/models/`

---

**סיום המדריך**




