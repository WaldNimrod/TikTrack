# דוח סטיות - Linked Items Service
## LINKED_ITEMS_SERVICE_DEVIATIONS_REPORT

**תאריך יצירה:** 26.11.2025, 4:57:43
**גרסה:** 1.0.0
**מטרה:** זיהוי שימושים מקומיים במקום Linked Items Service המרכזית

---

## 📊 סיכום כללי

- **סה"כ קבצים נסרקו:** 387
- **קבצים המשתמשים במערכת:** 27
- **קבצים עם בעיות:** 1
- **סה"כ בעיות נמצאו:** 1

### פילוח בעיות לפי סוג:

- **פונקציות הצגה מקומיות:** 0
- **פונקציות בדיקה מקומיות:** 0
- **פונקציות לוגיקה מקומיות:** 0
- **יצירת מודלים מקומית:** 1

### סטטוס טעינת המערכת:

- **linked-items.js ו-linked-items-service.js במוניפסט:** ✅ כן

---

## 📋 דוח מפורט לכל קובץ

### alerts-smart
**קובץ:** `alerts-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### alerts
**קובץ:** `alerts.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### alerts_backup_20251023_005535
**קובץ:** `alerts_backup_20251023_005535.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### background-tasks
**קובץ:** `background-tasks.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### button-color-mapping-simple
**קובץ:** `button-color-mapping-simple.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### button-color-mapping
**קובץ:** `button-color-mapping.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cache-management
**קובץ:** `cache-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cash_flows
**קובץ:** `cash_flows.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cash_flows_backup_20251023_004757
**קובץ:** `cash_flows_backup_20251023_004757.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### chart-management
**קובץ:** `chart-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### code-quality-dashboard
**קובץ:** `code-quality-dashboard.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### conditions-modals
**קובץ:** `conditions-modals.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### conditions-test
**קובץ:** `conditions-test.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### constraints
**קובץ:** `constraints.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### crud-testing-dashboard-smart
**קובץ:** `crud-testing-dashboard-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### crud-testing-dashboard
**קובץ:** `crud-testing-dashboard.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### css-management
**קובץ:** `css-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### data_import
**קובץ:** `data_import.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### db_display
**קובץ:** `db_display.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### db_extradata
**קובץ:** `db_extradata.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-actions-menu
**קובץ:** `debug-actions-menu.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### designs
**קובץ:** `designs.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### dynamic-colors-display
**קובץ:** `dynamic-colors-display.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### executions
**קובץ:** `executions.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### external-data-dashboard
**קובץ:** `external-data-dashboard.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### index-smart
**קובץ:** `index-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### index
**קובץ:** `index.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### init-system-management
**קובץ:** `init-system-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### add-ticker-modal
**קובץ:** `mockups/add-ticker-modal.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### comparative-analysis-page
**קובץ:** `mockups/daily-snapshots/comparative-analysis-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### date-comparison-modal
**קובץ:** `mockups/daily-snapshots/date-comparison-modal.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### economic-calendar-page
**קובץ:** `mockups/daily-snapshots/economic-calendar-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### emotional-tracking-widget
**קובץ:** `mockups/daily-snapshots/emotional-tracking-widget.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### heatmap-visual-example
**קובץ:** `mockups/daily-snapshots/heatmap-visual-example.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### history-widget
**קובץ:** `mockups/daily-snapshots/history-widget.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### portfolio-state-page
**קובץ:** `mockups/daily-snapshots/portfolio-state-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### price-history-page
**קובץ:** `mockups/daily-snapshots/price-history-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### strategy-analysis-page
**קובץ:** `mockups/daily-snapshots/strategy-analysis-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trading-journal-page
**קובץ:** `mockups/daily-snapshots/trading-journal-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tradingview-test-page
**קובץ:** `mockups/daily-snapshots/tradingview-test-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### flag-quick-action
**קובץ:** `mockups/flag-quick-action.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### watch-list-modal
**קובץ:** `mockups/watch-list-modal.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### watch-lists-page
**קובץ:** `mockups/watch-lists-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notes-smart
**קובץ:** `notes-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notes
**קובץ:** `notes.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notes_backup_20251023_005438
**קובץ:** `notes_backup_20251023_005438.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notifications-center
**קובץ:** `notifications-center.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-groups-management
**קובץ:** `preferences-groups-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-smart
**קובץ:** `preferences-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences
**קובץ:** `preferences.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### research
**קובץ:** `research.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### server-monitor
**קובץ:** `server-monitor.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### smart-init-testing
**קובץ:** `smart-init-testing.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### system-management
**קובץ:** `system-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tag-management
**קובץ:** `tag-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-frontend-wrappers
**קובץ:** `test-frontend-wrappers.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-header-only
**קובץ:** `test-header-only.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-monitoring
**קובץ:** `test-monitoring.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-nested-modal-rich-text
**קובץ:** `test-nested-modal-rich-text.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-phase1-recovery
**קובץ:** `test-phase1-recovery.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-phase3-1-comprehensive
**קובץ:** `test-phase3-1-comprehensive.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-quill
**קובץ:** `test-quill.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tickers
**קובץ:** `tickers.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tickers_backup_20251023_005345
**קובץ:** `tickers_backup_20251023_005345.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tooltip-editor
**קובץ:** `tooltip-editor.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trade_plans
**קובץ:** `trade_plans.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trades-smart
**קובץ:** `trades-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trades
**קובץ:** `trades.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trades_formatted
**קובץ:** `trades_formatted.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trading_accounts
**קובץ:** `trading_accounts.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trading_accounts_backup_20251023_005016
**קובץ:** `trading_accounts_backup_20251023_005016.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tradingview-widgets-showcase
**קובץ:** `tradingview-widgets-showcase.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### account-activity
**קובץ:** `scripts/account-activity.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### account-service
**קובץ:** `scripts/account-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### alert-service
**קובץ:** `scripts/alert-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### analyze-page-packages
**קובץ:** `scripts/analyze-page-packages.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### api-config
**קובץ:** `scripts/api-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### auth
**קובץ:** `scripts/auth.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### background-tasks
**קובץ:** `scripts/background-tasks.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### application-initializer
**קובץ:** `scripts/backup-old-initialization-systems/application-initializer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### master-initialization
**קובץ:** `scripts/backup-old-initialization-systems/master-initialization.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### smart-initialization
**קובץ:** `scripts/backup-old-initialization-systems/smart-initialization.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### button-color-checker
**קובץ:** `scripts/button-color-checker.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### button-helpers
**קובץ:** `scripts/button-helpers.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### button-icons
**קובץ:** `scripts/button-icons.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### button-icons.min
**קובץ:** `scripts/button-icons.min.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### button-system-demo-core
**קובץ:** `scripts/button-system-demo-core.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### button-system-init
**קובץ:** `scripts/button-system-init.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### button-tooltips-config
**קובץ:** `scripts/button-tooltips-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cache-clear-menu
**קובץ:** `scripts/cache-clear-menu.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cache-management
**קובץ:** `scripts/cache-management.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cache-policy-manager
**קובץ:** `scripts/cache-policy-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cache-sync-manager
**קובץ:** `scripts/cache-sync-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cache-ttl-guard
**קובץ:** `scripts/cache-ttl-guard.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### calendar-data-loader
**קובץ:** `scripts/calendar/calendar-data-loader.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### calendar-date-utils
**קובץ:** `scripts/calendar/calendar-date-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### calendar-renderer
**קובץ:** `scripts/calendar/calendar-renderer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### chart-management
**קובץ:** `scripts/chart-management.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### chart-renderer
**קובץ:** `scripts/chart-renderer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### chart-utils
**קובץ:** `scripts/chart-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### performance-adapter
**קובץ:** `scripts/charts/adapters/performance-adapter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trades-adapter
**קובץ:** `scripts/charts/adapters/trades-adapter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### chart-export
**קובץ:** `scripts/charts/chart-export.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### chart-loader
**קובץ:** `scripts/charts/chart-loader.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### chart-system
**קובץ:** `scripts/charts/chart-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### chart-theme
**קובץ:** `scripts/charts/chart-theme.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tradingview-adapter
**קובץ:** `scripts/charts/tradingview-adapter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tradingview-theme
**קובץ:** `scripts/charts/tradingview-theme.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### lightweight-charts.standalone.production
**קובץ:** `scripts/charts/vendor/lightweight-charts.standalone.production.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### check-conditions-usage
**קובץ:** `scripts/check-conditions-usage.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### check-duplicate-initialization
**קובץ:** `scripts/check-duplicate-initialization.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### check-info-summary-usage
**קובץ:** `scripts/check-info-summary-usage.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### check-pages-loading
**קובץ:** `scripts/check-pages-loading.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### check-select-populator-usage
**קובץ:** `scripts/check-select-populator-usage.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### check-translation-usage
**קובץ:** `scripts/check-translation-usage.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cluster-table
**קובץ:** `scripts/cluster-table.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### code-quality-dashboard
**קובץ:** `scripts/code-quality-dashboard.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### color-demo-toggle
**קובץ:** `scripts/color-demo-toggle.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### color-scheme-system-broken
**קובץ:** `scripts/color-scheme-system-broken.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### color-scheme-system-clean
**קובץ:** `scripts/color-scheme-system-clean.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### color-scheme-system
**קובץ:** `scripts/color-scheme-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### comparative-analysis-page
**קובץ:** `scripts/comparative-analysis-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### condition-translator
**קובץ:** `scripts/condition-translator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### conditions-test
**קובץ:** `scripts/conditions-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### constraint-manager
**קובץ:** `scripts/constraint-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### constraints
**קובץ:** `scripts/constraints.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### crud-response-handler-e2e-test
**קובץ:** `scripts/crud-response-handler-e2e-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### crud-testing-dashboard
**קובץ:** `scripts/crud-testing-dashboard.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### crud-testing-enhanced
**קובץ:** `scripts/crud-testing-enhanced.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### crud-utils
**קובץ:** `scripts/crud-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### css-duplicates-analyzer
**קובץ:** `scripts/css-duplicates-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### css-management
**קובץ:** `scripts/css-management.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### css-specificity-analyzer
**קובץ:** `scripts/css-specificity-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### data-collector
**קובץ:** `scripts/data-collector.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### data-utils
**קובץ:** `scripts/data-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### data_import
**קובץ:** `scripts/data_import.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### database
**קובץ:** `scripts/database.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### date-comparison-modal
**קובץ:** `scripts/date-comparison-modal.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### date-utils
**קובץ:** `scripts/date-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### db-extradata
**קובץ:** `scripts/db-extradata.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### db_display
**קובץ:** `scripts/db_display.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### db_extradata
**קובץ:** `scripts/db_extradata.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### missing-functions-scanner
**קובץ:** `scripts/debug/missing-functions-scanner.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### style-comparison-tool
**קובץ:** `scripts/debug/style-comparison-tool.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-account-colors
**קובץ:** `scripts/debug-account-colors.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-actions-menu-comprehensive
**קובץ:** `scripts/debug-actions-menu-comprehensive.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-active-session
**קובץ:** `scripts/debug-active-session.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-cash-flow-filter-console
**קובץ:** `scripts/debug-cash-flow-filter-console.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-cash-flow-filter
**קובץ:** `scripts/debug-cash-flow-filter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-currency-exchange-import
**קובץ:** `scripts/debug-currency-exchange-import.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-execution-ticker-complete
**קובץ:** `scripts/debug-execution-ticker-complete.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-execution-ticker-loading
**קובץ:** `scripts/debug-execution-ticker-loading.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-filter-tooltips-comprehensive
**קובץ:** `scripts/debug-filter-tooltips-comprehensive.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-filter-tooltips
**קובץ:** `scripts/debug-filter-tooltips.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-import-filtering
**קובץ:** `scripts/debug-import-filtering.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-import-steps
**קובץ:** `scripts/debug-import-steps.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-modal-stacking
**קובץ:** `scripts/debug-modal-stacking.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-modal-z-index
**קובץ:** `scripts/debug-modal-z-index.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-notes-summary
**קובץ:** `scripts/debug-notes-summary.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-populate-form-dates
**קובץ:** `scripts/debug-populate-form-dates.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-showEditTradingAccountModalById
**קובץ:** `scripts/debug-showEditTradingAccountModalById.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-summary-card
**קובץ:** `scripts/debug-summary-card.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-tooltips
**קובץ:** `scripts/debug-tooltips.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-trade-fields
**קובץ:** `scripts/debug-trade-fields.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### designs
**קובץ:** `scripts/designs.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### dynamic-colors-display
**קובץ:** `scripts/dynamic-colors-display.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### economic-calendar-page
**קובץ:** `scripts/economic-calendar-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### emotional-tracking-widget
**קובץ:** `scripts/emotional-tracking-widget.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### index-enhancements
**קובץ:** `scripts/enhancements/index-enhancements.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### entity-details-api
**קובץ:** `scripts/entity-details-api.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### error-handlers
**קובץ:** `scripts/error-handlers.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### external-data-dashboard
**קובץ:** `scripts/external-data-dashboard.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### external-data-service
**קובץ:** `scripts/external-data-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### external-data-settings-service
**קובץ:** `scripts/external-data-settings-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### global-favicon
**קובץ:** `scripts/global-favicon.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### header-system-old
**קובץ:** `scripts/header-system-old.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### header-system-v2
**קובץ:** `scripts/header-system-v2.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### header-system
**קובץ:** `scripts/header-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### history-widget
**קובץ:** `scripts/history-widget.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### icon-mappings
**קובץ:** `scripts/icon-mappings.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### icon-replacement-helper
**קובץ:** `scripts/icon-replacement-helper.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### icon-system
**קובץ:** `scripts/icon-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### import-user-data
**קובץ:** `scripts/import-user-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### index
**קובץ:** `scripts/index.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### info-summary-configs
**קובץ:** `scripts/info-summary-configs.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### info-summary-system
**קובץ:** `scripts/info-summary-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### all-pages-monitoring-test
**קובץ:** `scripts/init-system/all-pages-monitoring-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### comprehensive-initialization-test
**קובץ:** `scripts/init-system/comprehensive-initialization-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### dependency-analyzer
**קובץ:** `scripts/init-system/dependency-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### code-cleanup-analyzer
**קובץ:** `scripts/init-system/dev-tools/code-cleanup-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### final-validator
**קובץ:** `scripts/init-system/dev-tools/final-validator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### page-template-generator
**קובץ:** `scripts/init-system/dev-tools/page-template-generator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### performance-analyzer
**קובץ:** `scripts/init-system/dev-tools/performance-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### script-analyzer
**קובץ:** `scripts/init-system/dev-tools/script-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### initialization-checker
**קובץ:** `scripts/init-system/initialization-checker.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### load-order-validator
**קובץ:** `scripts/init-system/load-order-validator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### package-manifest-new
**קובץ:** `scripts/init-system/package-manifest-new.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### package-manifest-old
**קובץ:** `scripts/init-system/package-manifest-old.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### page-health-checker
**קובץ:** `scripts/init-system/page-health-checker.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### pages-standardization-plan
**קובץ:** `scripts/init-system/pages-standardization-plan.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### validate-package-manifest
**קובץ:** `scripts/init-system/validate-package-manifest.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### runtime-validator
**קובץ:** `scripts/init-system/validators/runtime-validator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### init-system-check
**קובץ:** `scripts/init-system-check.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### init-system-management
**קובץ:** `scripts/init-system-management.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### linter-realtime-monitor
**קובץ:** `scripts/linter-realtime-monitor.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### log-recovery
**קובץ:** `scripts/log-recovery.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### logger-service
**קובץ:** `scripts/logger-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### migration-helper
**קובץ:** `scripts/migration-helper.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### migration-testing-suite
**קובץ:** `scripts/migration-testing-suite.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### economic-events-mock-data
**קובץ:** `scripts/mock-data/economic-events-mock-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trade-history-mock-data
**קובץ:** `scripts/mock-data/trade-history-mock-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### mockups-icon-initializer
**קובץ:** `scripts/mockups-icon-initializer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### alerts-config
**קובץ:** `scripts/modal-configs/alerts-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cash-flows-config
**קובץ:** `scripts/modal-configs/cash-flows-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### conditions-config
**קובץ:** `scripts/modal-configs/conditions-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### executions-config
**קובץ:** `scripts/modal-configs/executions-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notes-config
**קובץ:** `scripts/modal-configs/notes-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tag-management-config
**קובץ:** `scripts/modal-configs/tag-management-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tag-search-config
**קובץ:** `scripts/modal-configs/tag-search-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tickers-config
**קובץ:** `scripts/modal-configs/tickers-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trade-plans-config
**קובץ:** `scripts/modal-configs/trade-plans-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trades-config
**קובץ:** `scripts/modal-configs/trades-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trading-accounts-config
**קובץ:** `scripts/modal-configs/trading-accounts-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### modal-navigation-manager
**קובץ:** `scripts/modal-navigation-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### modal-navigation-monitor
**קובץ:** `scripts/modal-navigation-monitor.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### actions-menu-system
**קובץ:** `scripts/modules/actions-menu-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### communication-module
**קובץ:** `scripts/modules/communication-module.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### core-systems
**קובץ:** `scripts/modules/core-systems.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### data-advanced
**קובץ:** `scripts/modules/data-advanced.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### data-basic
**קובץ:** `scripts/modules/data-basic.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### dynamic-loader-config
**קובץ:** `scripts/modules/dynamic-loader-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### localstorage-sync
**קובץ:** `scripts/modules/localstorage-sync.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notification-templates
**קובץ:** `scripts/modules/notification-templates.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### ui-advanced
**קובץ:** `scripts/modules/ui-advanced.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### monitor-import-execution
**קובץ:** `scripts/monitor-import-execution.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### monitoring-functions
**קובץ:** `scripts/monitoring-functions.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notification-category-detector
**קובץ:** `scripts/notification-category-detector.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notification-system-tester
**קובץ:** `scripts/notification-system-tester.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notification-system
**קובץ:** `scripts/notification-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notifications-center
**קובץ:** `scripts/notifications-center.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### page-initialization-configs
**קובץ:** `scripts/page-initialization-configs.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### page-state-e2e-test
**קובץ:** `scripts/page-state-e2e-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### page-state-manager
**קובץ:** `scripts/page-state-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### page-utils
**קובץ:** `scripts/page-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### pagination-system
**קובץ:** `scripts/pagination-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### pending-execution-trade-creation
**קובץ:** `scripts/pending-execution-trade-creation.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### pending-executions-widget
**קובץ:** `scripts/pending-executions-widget.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### pending-trade-plan-widget
**קובץ:** `scripts/pending-trade-plan-widget.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### portfolio-state-page
**קובץ:** `scripts/portfolio-state-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### positions-portfolio
**קובץ:** `scripts/positions-portfolio.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-colors
**קובץ:** `scripts/preferences-colors.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-core-new
**קובץ:** `scripts/preferences-core-new.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-debug-monitor
**קובץ:** `scripts/preferences-debug-monitor.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-group-manager
**קובץ:** `scripts/preferences-group-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-lazy-loader
**קובץ:** `scripts/preferences-lazy-loader.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-page
**קובץ:** `scripts/preferences-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-pages-scanner
**קובץ:** `scripts/preferences-pages-scanner.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-profiles
**קובץ:** `scripts/preferences-profiles.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-refresh-controls
**קובץ:** `scripts/preferences-refresh-controls.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-refresh-monitor
**קובץ:** `scripts/preferences-refresh-monitor.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-ui-v4
**קובץ:** `scripts/preferences-ui-v4.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-ui
**קובץ:** `scripts/preferences-ui.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-validation
**קובץ:** `scripts/preferences-validation.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences
**קובץ:** `scripts/preferences.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### price-history-page
**קובץ:** `scripts/price-history-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### quick-quality-check
**קובץ:** `scripts/quick-quality-check.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### realtime-notifications-client
**קובץ:** `scripts/realtime-notifications-client.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### register-table-portfolio
**קובץ:** `scripts/register-table-portfolio.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### research
**קובץ:** `scripts/research.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### server-monitor
**קובץ:** `scripts/server-monitor.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### alert-condition-renderer
**קובץ:** `scripts/services/alert-condition-renderer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### alerts-data
**קובץ:** `scripts/services/alerts-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cash-flows-data
**קובץ:** `scripts/services/cash-flows-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### crud-response-handler
**קובץ:** `scripts/services/crud-response-handler.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### dashboard-data
**קובץ:** `scripts/services/dashboard-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### data-collection-service
**קובץ:** `scripts/services/data-collection-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### data-import-data
**קובץ:** `scripts/services/data-import-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### default-value-setter
**קובץ:** `scripts/services/default-value-setter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### executions-data
**קובץ:** `scripts/services/executions-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### field-renderer-service
**קובץ:** `scripts/services/field-renderer-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### investment-calculation-service
**קובץ:** `scripts/services/investment-calculation-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### lint-status-service
**קובץ:** `scripts/services/lint-status-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### notes-data
**קובץ:** `scripts/services/notes-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-data
**קובץ:** `scripts/services/preferences-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### preferences-v4
**קובץ:** `scripts/services/preferences-v4.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### research-data
**קובץ:** `scripts/services/research-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### rich-text-editor-service
**קובץ:** `scripts/services/rich-text-editor-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### select-populator-service
**קובץ:** `scripts/services/select-populator-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### statistics-calculator
**קובץ:** `scripts/services/statistics-calculator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### table-sort-value-adapter
**קובץ:** `scripts/services/table-sort-value-adapter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tag-service
**קובץ:** `scripts/services/tag-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tickers-data
**קובץ:** `scripts/services/tickers-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trade-plans-data
**קובץ:** `scripts/services/trade-plans-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trades-data
**קובץ:** `scripts/services/trades-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trading-accounts-data
**קובץ:** `scripts/services/trading-accounts-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### unified-crud-service
**קובץ:** `scripts/services/unified-crud-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ✅ כן
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 334:** יצירת מודל מקומי לפריטים מקושרים במקום showLinkedItemsModal
   - **סוג:** localModalCreation
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `getElementById('linkedItemsModal')`

---

### services-test
**קובץ:** `scripts/services-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### standardize-pages
**קובץ:** `scripts/standardize-pages.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### strategy-analysis-page
**קובץ:** `scripts/strategy-analysis-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-base
**קובץ:** `scripts/system-management/core/sm-base.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-error-handler
**קובץ:** `scripts/system-management/core/sm-error-handler.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-ui-components
**קובץ:** `scripts/system-management/core/sm-ui-components.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-section-alerts
**קובץ:** `scripts/system-management/sections/sm-section-alerts.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-section-background-tasks
**קובץ:** `scripts/system-management/sections/sm-section-background-tasks.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-section-cache
**קובץ:** `scripts/system-management/sections/sm-section-cache.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-section-dashboard
**קובץ:** `scripts/system-management/sections/sm-section-dashboard.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-section-database
**קובץ:** `scripts/system-management/sections/sm-section-database.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-section-external-data
**קובץ:** `scripts/system-management/sections/sm-section-external-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-section-operations
**קובץ:** `scripts/system-management/sections/sm-section-operations.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-section-performance
**קובץ:** `scripts/system-management/sections/sm-section-performance.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### sm-section-server
**קובץ:** `scripts/system-management/sections/sm-section-server.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### system-management-main
**קובץ:** `scripts/system-management/system-management-main.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### system-management
**קובץ:** `scripts/system-management.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### table-data-registry
**קובץ:** `scripts/table-data-registry.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### table-mappings
**קובץ:** `scripts/table-mappings.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tag-events
**קובץ:** `scripts/tag-events.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tag-ui-manager
**קובץ:** `scripts/tag-ui-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-db-display-page-loading
**קובץ:** `scripts/test-db-display-page-loading.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-executions-page-loading
**קובץ:** `scripts/test-executions-page-loading.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-header-only
**קובץ:** `scripts/test-header-only.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-monitoring
**קובץ:** `scripts/test-monitoring.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-positions-portfolio-comprehensive
**קובץ:** `scripts/test-positions-portfolio-comprehensive.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-preferences-debug
**קובץ:** `scripts/test-preferences-debug.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-preferences-loading-across-pages
**קובץ:** `scripts/test-preferences-loading-across-pages.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-preferences-loading-complete
**קובץ:** `scripts/test-preferences-loading-complete.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-runner
**קובץ:** `scripts/test-runner.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test-unified-table-system
**קובץ:** `scripts/test-unified-table-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### comprehensive-cache-clearing-test
**קובץ:** `scripts/testing/comprehensive-cache-clearing-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test_cache_system_integration
**קובץ:** `scripts/testing/test_cache_system_integration.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test_frontend_wrappers
**קובץ:** `scripts/testing/test_frontend_wrappers.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test_initialization_stages
**קובץ:** `scripts/testing/test_initialization_stages.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test_packages_and_page_configs
**קובץ:** `scripts/testing/test_packages_and_page_configs.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### test_preferences_loading_events
**קובץ:** `scripts/testing/test_preferences_loading_events.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### debug-cashflow-account
**קובץ:** `scripts/tests/debug-cashflow-account.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### edit-modal-test
**קובץ:** `scripts/tests/edit-modal-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### field-mapping-validator
**קובץ:** `scripts/tests/field-mapping-validator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### ticker-service
**קובץ:** `scripts/ticker-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trade-history-page
**קובץ:** `scripts/trade-history-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trade-plan-service
**קובץ:** `scripts/trade-plan-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trade-selector-modal
**קובץ:** `scripts/trade-selector-modal.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trades-adapter
**קובץ:** `scripts/trades-adapter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### trading-journal-page
**קובץ:** `scripts/trading-journal-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tradingview-test-page
**קובץ:** `scripts/tradingview-test-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tradingview-widgets-colors
**קובץ:** `scripts/tradingview-widgets/tradingview-widgets-colors.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tradingview-widgets-config
**קובץ:** `scripts/tradingview-widgets/tradingview-widgets-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tradingview-widgets-core
**קובץ:** `scripts/tradingview-widgets/tradingview-widgets-core.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### tradingview-widgets-factory
**קובץ:** `scripts/tradingview-widgets/tradingview-widgets-factory.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### translation-utils
**קובץ:** `scripts/translation-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### unified-app-initializer
**קובץ:** `scripts/unified-app-initializer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### unified-cache-manager
**קובץ:** `scripts/unified-cache-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### unified-log-api
**קובץ:** `scripts/unified-log-api.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### unified-log-display
**קובץ:** `scripts/unified-log-display.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### unified-log-manager
**קובץ:** `scripts/unified-log-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### unified-table-system
**קובץ:** `scripts/unified-table-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### business-logic-batch-helper
**קובץ:** `scripts/utils/business-logic-batch-helper.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### cache-key-helper
**קובץ:** `scripts/utils/cache-key-helper.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### request-deduplication-helper
**קובץ:** `scripts/utils/request-deduplication-helper.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### validation-utils
**קובץ:** `scripts/validation-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### warning-system
**קובץ:** `scripts/warning-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### recent-trades-widget
**קובץ:** `scripts/widgets/recent-trades-widget.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

### yahoo-finance-service
**קובץ:** `scripts/yahoo-finance-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך entity-services package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בפריטים מקושרים

---

## 🎯 סיכום והמלצות

### בעיות קריטיות (גבוהה חומרה):
- **scripts/services/unified-crud-service.js** - 1 בעיות

### בעיות בינוניות:
- אין בעיות בינוניות

---

**תאריך עדכון אחרון:** 26.11.2025, 4:57:43
