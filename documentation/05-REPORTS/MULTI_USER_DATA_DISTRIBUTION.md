# חלוקת נתונים בין משתמשים - מערכת Multi-User
## Multi-User Data Distribution Guide

**תאריך יצירה:** ינואר 2025  
**גרסה:** 1.0.0  
**מטרה:** הגדרת חלוקת נתוני דוגמה בין שלושת המשתמשים במערכת

---

## 📋 תוכן עניינים

1. [משתמשים במערכת](#משתמשים-במערכת)
2. [חלוקת נתונים](#חלוקת-נתונים)
3. [פרטי התחברות](#פרטי-התחברות)
4. [שימוש בסקריפטים](#שימוש-בסקריפטים)

---

## 👥 משתמשים במערכת

המערכת מכילה שלושה משתמשים עם פרופילים שונים:

### 1. משתמש (User)
- **Username:** `user`
- **Password:** `user123`
- **תפקיד:** משתמש רגיל
- **פרופיל נתונים:** שפע נתונים - כל נתוני הדוגמה

### 2. מנהל (Admin)
- **Username:** `admin`
- **Password:** `admin123`
- **תפקיד:** משתמש מנהל
- **פרופיל נתונים:** נתוני דוגמה מוגבלים - דוגמאות בסיסיות

### 3. נימרוד (Nimrod)
- **Username:** `nimrod`
- **Password:** `nimw`
- **תפקיד:** מנהל ראשי (`is_default=True`)
- **פרופיל נתונים:** נקי - ללא נתונים כלל

---

## 📊 חלוקת נתונים

### משתמש (User) - שפע נתונים

**כל נתוני הדוגמה נוצרים למשתמש זה:**

- **50 טיקרים** - כל הטיקרים שנוצרים
- **2 חשבונות מסחר:**
  - חשבון ראשי: 70% מהפעילות
  - חשבון מטבע אחר: 30% מהפעילות
- **120 תוכניות טרייד** - כל התוכניות
- **80 טריידים** - כל הטריידים
- **ביצועים** - כל הביצועים
- **תזרימי מזומן** - כל תזרימי המזומן
- **התראות** - כל ההתראות
- **הערות** - כל ההערות
- **ניתוחי AI** - כל ניתוחי ה-AI
- **שיוך טיקרים** - כל הטיקרים משויכים למשתמש זה

### מנהל (Admin) - נתוני דוגמה מוגבלים

**נתונים בסיסיים לדוגמה:**

- **10 טיקרים** - טיקרים נבחרים (AAPL, MSFT, GOOGL, וכו')
- **1 חשבון מסחר:**
  - חשבון ראשי: מטבע USD
- **20 תוכניות טרייד** - דוגמאות בסיסיות
- **15 טריידים** - דוגמאות בסיסיות
- **ביצועים** - ביצועים בסיסיים
- **תזרימי מזומן** - תזרימי מזומן בסיסיים
- **התראות** - 5 התראות לדוגמה
- **הערות** - 5 הערות לדוגמה
- **ניתוחי AI** - 2-3 ניתוחי AI לדוגמה
- **שיוך טיקרים** - רק הטיקרים שנוצרו משויכים למשתמש זה

### נימרוד (Nimrod) - נקי, ללא נתונים

**אין נתונים - משתמש נקי:**

- **0 טיקרים** - אין טיקרים (רק SPY קיים במערכת, לא משויך)
- **0 חשבונות מסחר**
- **0 תוכניות טרייד**
- **0 טריידים**
- **0 ביצועים**
- **0 תזרימי מזומן**
- **0 התראות**
- **0 הערות**
- **0 ניתוחי AI**
- **0 שיוכי טיקרים**

**הערות:**
- משתמש זה משמש לבדיקות נקיות
- ניתן ליצור נתונים חדשים דרך הממשק
- משמש להדגמת מערכת ללא נתונים

---

## 🔑 פרטי התחברות

### משתמש (User)
```
Username: user
Password: user123
```

### מנהל (Admin)
```
Username: admin
Password: admin123
```

### נימרוד (Nimrod)
```
Username: nimrod
Password: nimw
```

---

## 🚀 שימוש בסקריפטים

### יצירת נתונים למשתמש יחיד

**הסקריפט `generate_demo_data.py` יוצר נתונים למשתמש ספציפי:**

```bash
# יצירת נתוני דוגמה למשתמש ספציפי
python3 Backend/scripts/generate_demo_data.py --username user

# יצירת נתוני דוגמה למשתמש admin
python3 Backend/scripts/generate_demo_data.py --username admin

# ללא username - משתמש ראשון שנמצא (תאימות לאחור)
python3 Backend/scripts/generate_demo_data.py
```

**הערות:**
- הסקריפט תומך בפרמטר `--username` ליצירת נתונים למשתמש ספציפי
- אם לא מסופק `--username`, הסקריפט ישתמש במשתמש הראשון שנמצא (תאימות לאחור)
- כל הנתונים נוצרים למשתמש שצוין

### יצירת נתונים לכל המשתמשים (מומלץ)

**הסקריפט `generate_multi_user_demo_data.py` יוצר נתונים לכל המשתמשים:**

```bash
# יצירת נתוני דוגמה לכל המשתמשים
python3 Backend/scripts/generate_multi_user_demo_data.py

# Dry run - רק אימות ללא יצירת נתונים
python3 Backend/scripts/generate_multi_user_demo_data.py --dry-run

# עם הודעות מפורטות
python3 Backend/scripts/generate_multi_user_demo_data.py --verbose
```

**הערות:**
- הסקריפט יוצר נתונים לפי ההגדרות במסמך זה
- משתמש (user) - כל הנתונים (120 תוכניות, 80 טריידים, 50 טיקרים)
- מנהל (admin) - נתונים מוגבלים (20 תוכניות, 15 טריידים, 10 טיקרים)
- נימרוד (nimrod) - ללא נתונים (מדלג)
- זהו הסקריפט המומלץ ליצירת נתוני דוגמה במערכת multi-user

---

## 📝 הערות חשובות

### שיוך נתונים למשתמשים

- **כל הנתונים משויכים למשתמש דרך `user_id`**
- **טיקרים משויכים דרך `user_tickers`**
- **ניתוחי AI משויכים דרך `user_id` ב-`ai_analysis_requests`**

### מפתחות API

- **מפתחות API נשמרים ב-`user_llm_providers`**
- **לא נמחקים בתהליך הניקוי**
- **כל משתמש יכול להגדיר מפתחות API שלו**

### בדיקות

- **יש לבדוק שכל משתמש רואה רק את הנתונים שלו**
- **יש לבדוק שאין חפיפה בנתונים בין משתמשים**
- **יש לבדוק שכל API endpoint מחזיר רק נתונים של המשתמש המחובר**

---

## 🔗 קישורים

- **מסמך תהליך הניקוי:** [USER_DATA_CLEANUP_PROCESS.md](USER_DATA_CLEANUP_PROCESS.md)
- **מדריך יצירת נתוני דוגמה:** [DEMO_DATA_GENERATION_GUIDE.md](DEMO_DATA_GENERATION_GUIDE.md)
- **סקריפט יצירת משתמשים:** `Backend/scripts/setup_initial_users.py`

---

**סיום המסמך**


