# דוח סטיות - Translation Utilities
## TRANSLATION_UTILITIES_DEVIATIONS_REPORT

**תאריך יצירה:** 26.11.2025, 3:53:16
**גרסה:** 1.0.0
**מטרה:** זיהוי שימושים מקומיים במקום Translation Utilities המרכזית

---

## 📊 סיכום כללי

- **סה"כ קבצים נסרקו:** 381
- **קבצים המשתמשים במערכת:** 15
- **קבצים עם בעיות:** 22
- **סה"כ בעיות נמצאו:** 37

### פילוח בעיות לפי סוג:

- **פונקציות תרגום מקומיות:** 2
- **מפות תרגום מקומיות:** 18
- **פונקציות פורמט מקומיות:** 17

### סטטוס טעינת המערכת:

- **translation-utils.js במוניפסט:** ✅ כן

---

## 📋 דוח מפורט לכל קובץ

### alerts-smart
**קובץ:** `alerts-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### alerts
**קובץ:** `alerts.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### alerts_backup_20251023_005535
**קובץ:** `alerts_backup_20251023_005535.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### background-tasks
**קובץ:** `background-tasks.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### button-color-mapping-simple
**קובץ:** `button-color-mapping-simple.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### button-color-mapping
**קובץ:** `button-color-mapping.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cache-management
**קובץ:** `cache-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cash_flows
**קובץ:** `cash_flows.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cash_flows_backup_20251023_004757
**קובץ:** `cash_flows_backup_20251023_004757.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### chart-management
**קובץ:** `chart-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### code-quality-dashboard
**קובץ:** `code-quality-dashboard.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### conditions-modals
**קובץ:** `conditions-modals.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### conditions-test
**קובץ:** `conditions-test.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### constraints
**קובץ:** `constraints.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### crud-testing-dashboard-smart
**קובץ:** `crud-testing-dashboard-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### crud-testing-dashboard
**קובץ:** `crud-testing-dashboard.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### css-management
**קובץ:** `css-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### data_import
**קובץ:** `data_import.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### db_display
**קובץ:** `db_display.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### db_extradata
**קובץ:** `db_extradata.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-actions-menu
**קובץ:** `debug-actions-menu.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### designs
**קובץ:** `designs.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### dynamic-colors-display
**קובץ:** `dynamic-colors-display.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### executions
**קובץ:** `executions.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### external-data-dashboard
**קובץ:** `external-data-dashboard.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### index-smart
**קובץ:** `index-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### index
**קובץ:** `index.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### init-system-management
**קובץ:** `init-system-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### comparative-analysis-page
**קובץ:** `mockups/daily-snapshots/comparative-analysis-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### date-comparison-modal
**קובץ:** `mockups/daily-snapshots/date-comparison-modal.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### economic-calendar-page
**קובץ:** `mockups/daily-snapshots/economic-calendar-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### emotional-tracking-widget
**קובץ:** `mockups/daily-snapshots/emotional-tracking-widget.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### heatmap-visual-example
**קובץ:** `mockups/daily-snapshots/heatmap-visual-example.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### history-widget
**קובץ:** `mockups/daily-snapshots/history-widget.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### portfolio-state-page
**קובץ:** `mockups/daily-snapshots/portfolio-state-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### price-history-page
**קובץ:** `mockups/daily-snapshots/price-history-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### strategy-analysis-page
**קובץ:** `mockups/daily-snapshots/strategy-analysis-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trade-history-page
**קובץ:** `mockups/daily-snapshots/trade-history-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trading-journal-page
**קובץ:** `mockups/daily-snapshots/trading-journal-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tradingview-test-page
**קובץ:** `mockups/daily-snapshots/tradingview-test-page.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notes-smart
**קובץ:** `notes-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notes
**קובץ:** `notes.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notes_backup_20251023_005438
**קובץ:** `notes_backup_20251023_005438.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notifications-center
**קובץ:** `notifications-center.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-groups-management
**קובץ:** `preferences-groups-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-smart
**קובץ:** `preferences-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences
**קובץ:** `preferences.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### research
**קובץ:** `research.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### server-monitor
**קובץ:** `server-monitor.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### smart-init-testing
**קובץ:** `smart-init-testing.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### system-management
**קובץ:** `system-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tag-management
**קובץ:** `tag-management.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-frontend-wrappers
**קובץ:** `test-frontend-wrappers.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-header-only
**קובץ:** `test-header-only.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-monitoring
**קובץ:** `test-monitoring.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-nested-modal-rich-text
**קובץ:** `test-nested-modal-rich-text.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-phase1-recovery
**קובץ:** `test-phase1-recovery.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-phase3-1-comprehensive
**קובץ:** `test-phase3-1-comprehensive.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-quill
**קובץ:** `test-quill.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tickers
**קובץ:** `tickers.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tickers_backup_20251023_005345
**קובץ:** `tickers_backup_20251023_005345.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tooltip-editor
**קובץ:** `tooltip-editor.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trade_plans
**קובץ:** `trade_plans.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trades-smart
**קובץ:** `trades-smart.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trades
**קובץ:** `trades.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trades_formatted
**קובץ:** `trades_formatted.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trading_accounts
**קובץ:** `trading_accounts.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trading_accounts_backup_20251023_005016
**קובץ:** `trading_accounts_backup_20251023_005016.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tradingview-widgets-showcase
**קובץ:** `tradingview-widgets-showcase.html`
**קטגוריה:** HTML

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### account-activity
**קובץ:** `scripts/account-activity.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 959:** פונקציית פורמט מקומית: formatAmount
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatAmount(`

---

### account-service
**קובץ:** `scripts/account-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### active-alerts-component
**קובץ:** `scripts/active-alerts-component.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### alert-service
**קובץ:** `scripts/alert-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### alerts
**קובץ:** `scripts/alerts.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### analyze-page-packages
**קובץ:** `scripts/analyze-page-packages.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### api-config
**קובץ:** `scripts/api-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### auth
**קובץ:** `scripts/auth.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### background-tasks
**קובץ:** `scripts/background-tasks.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### application-initializer
**קובץ:** `scripts/backup-old-initialization-systems/application-initializer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### master-initialization
**קובץ:** `scripts/backup-old-initialization-systems/master-initialization.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### smart-initialization
**קובץ:** `scripts/backup-old-initialization-systems/smart-initialization.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### button-color-checker
**קובץ:** `scripts/button-color-checker.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### button-helpers
**קובץ:** `scripts/button-helpers.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### button-icons
**קובץ:** `scripts/button-icons.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### button-icons.min
**קובץ:** `scripts/button-icons.min.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### button-system-demo-core
**קובץ:** `scripts/button-system-demo-core.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### button-system-init
**קובץ:** `scripts/button-system-init.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### button-tooltips-config
**קובץ:** `scripts/button-tooltips-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cache-clear-menu
**קובץ:** `scripts/cache-clear-menu.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cache-management
**קובץ:** `scripts/cache-management.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cache-policy-manager
**קובץ:** `scripts/cache-policy-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cache-sync-manager
**קובץ:** `scripts/cache-sync-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cache-ttl-guard
**קובץ:** `scripts/cache-ttl-guard.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### calendar-data-loader
**קובץ:** `scripts/calendar/calendar-data-loader.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### calendar-date-utils
**קובץ:** `scripts/calendar/calendar-date-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### calendar-renderer
**קובץ:** `scripts/calendar/calendar-renderer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cash_flows
**קובץ:** `scripts/cash_flows.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ✅ כן
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 2179:** פונקציית פורמט מקומית: formatAmount
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatAmount(`

---

### chart-management
**קובץ:** `scripts/chart-management.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### chart-renderer
**קובץ:** `scripts/chart-renderer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### chart-utils
**קובץ:** `scripts/chart-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### performance-adapter
**קובץ:** `scripts/charts/adapters/performance-adapter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trades-adapter
**קובץ:** `scripts/charts/adapters/trades-adapter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### chart-export
**קובץ:** `scripts/charts/chart-export.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### chart-loader
**קובץ:** `scripts/charts/chart-loader.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### chart-system
**קובץ:** `scripts/charts/chart-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### chart-theme
**קובץ:** `scripts/charts/chart-theme.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tradingview-adapter
**קובץ:** `scripts/charts/tradingview-adapter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tradingview-theme
**קובץ:** `scripts/charts/tradingview-theme.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### lightweight-charts.standalone.production
**קובץ:** `scripts/charts/vendor/lightweight-charts.standalone.production.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### check-conditions-usage
**קובץ:** `scripts/check-conditions-usage.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### check-duplicate-initialization
**קובץ:** `scripts/check-duplicate-initialization.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### check-info-summary-usage
**קובץ:** `scripts/check-info-summary-usage.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### check-pages-loading
**קובץ:** `scripts/check-pages-loading.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### check-select-populator-usage
**קובץ:** `scripts/check-select-populator-usage.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cluster-table
**קובץ:** `scripts/cluster-table.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ✅ כן
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 173:** מפת תרגום מקומית: actionMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `const actionMap = {`

2. **שורה 173:** מפת תרגום מקומית: actionMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `actionMap = {`

---

### code-quality-dashboard
**קובץ:** `scripts/code-quality-dashboard.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### color-demo-toggle
**קובץ:** `scripts/color-demo-toggle.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### color-scheme-system-broken
**קובץ:** `scripts/color-scheme-system-broken.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### color-scheme-system-clean
**קובץ:** `scripts/color-scheme-system-clean.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### color-scheme-system
**קובץ:** `scripts/color-scheme-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### comparative-analysis-page
**קובץ:** `scripts/comparative-analysis-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 1069:** פונקציית פורמט מקומית: formatCurrency
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatCurrency(`

---

### condition-translator
**קובץ:** `scripts/condition-translator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### conditions-test
**קובץ:** `scripts/conditions-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### constraint-manager
**קובץ:** `scripts/constraint-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### constraints
**קובץ:** `scripts/constraints.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### crud-response-handler-e2e-test
**קובץ:** `scripts/crud-response-handler-e2e-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### crud-testing-dashboard
**קובץ:** `scripts/crud-testing-dashboard.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### crud-testing-enhanced
**קובץ:** `scripts/crud-testing-enhanced.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### crud-utils
**קובץ:** `scripts/crud-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### css-duplicates-analyzer
**קובץ:** `scripts/css-duplicates-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### css-management
**קובץ:** `scripts/css-management.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### css-specificity-analyzer
**קובץ:** `scripts/css-specificity-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### currencies
**קובץ:** `scripts/currencies.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### data-collector
**קובץ:** `scripts/data-collector.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### data-utils
**קובץ:** `scripts/data-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### data_import
**קובץ:** `scripts/data_import.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### database
**קובץ:** `scripts/database.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 481:** מפת תרגום מקומית: statusMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `const statusMap = {`

2. **שורה 481:** מפת תרגום מקומית: statusMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `statusMap = {`

3. **שורה 438:** פונקציית פורמט מקומית: formatNumber
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatNumber(`

4. **שורה 456:** פונקציית פורמט מקומית: formatCurrency
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatCurrency(`

---

### date-comparison-modal
**קובץ:** `scripts/date-comparison-modal.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 775:** פונקציית פורמט מקומית: formatCurrency
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatCurrency(`

---

### date-utils
**קובץ:** `scripts/date-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### db-extradata
**קובץ:** `scripts/db-extradata.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### db_display
**קובץ:** `scripts/db_display.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### db_extradata
**קובץ:** `scripts/db_extradata.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### missing-functions-scanner
**קובץ:** `scripts/debug/missing-functions-scanner.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### style-comparison-tool
**קובץ:** `scripts/debug/style-comparison-tool.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-account-colors
**קובץ:** `scripts/debug-account-colors.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-actions-menu-comprehensive
**קובץ:** `scripts/debug-actions-menu-comprehensive.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-active-session
**קובץ:** `scripts/debug-active-session.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-cash-flow-filter-console
**קובץ:** `scripts/debug-cash-flow-filter-console.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-cash-flow-filter
**קובץ:** `scripts/debug-cash-flow-filter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-currency-exchange-import
**קובץ:** `scripts/debug-currency-exchange-import.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-execution-ticker-complete
**קובץ:** `scripts/debug-execution-ticker-complete.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-execution-ticker-loading
**קובץ:** `scripts/debug-execution-ticker-loading.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-filter-tooltips-comprehensive
**קובץ:** `scripts/debug-filter-tooltips-comprehensive.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-filter-tooltips
**קובץ:** `scripts/debug-filter-tooltips.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-import-filtering
**קובץ:** `scripts/debug-import-filtering.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-import-steps
**קובץ:** `scripts/debug-import-steps.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-modal-stacking
**קובץ:** `scripts/debug-modal-stacking.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-modal-z-index
**קובץ:** `scripts/debug-modal-z-index.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-notes-summary
**קובץ:** `scripts/debug-notes-summary.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-populate-form-dates
**קובץ:** `scripts/debug-populate-form-dates.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-showEditTradingAccountModalById
**קובץ:** `scripts/debug-showEditTradingAccountModalById.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-summary-card
**קובץ:** `scripts/debug-summary-card.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-tooltips-console
**קובץ:** `scripts/debug-tooltips-console.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-tooltips
**קובץ:** `scripts/debug-tooltips.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-trade-fields
**קובץ:** `scripts/debug-trade-fields.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### designs
**קובץ:** `scripts/designs.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### dynamic-colors-display
**קובץ:** `scripts/dynamic-colors-display.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### economic-calendar-page
**קובץ:** `scripts/economic-calendar-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### emotional-tracking-widget
**קובץ:** `scripts/emotional-tracking-widget.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### index-enhancements
**קובץ:** `scripts/enhancements/index-enhancements.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### entity-details-api
**קובץ:** `scripts/entity-details-api.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### entity-details-modal
**קובץ:** `scripts/entity-details-modal.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### entity-details-renderer
**קובץ:** `scripts/entity-details-renderer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ✅ כן
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 1509:** פונקציית פורמט מקומית: formatAmount
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `const formatAmount =`

---

### error-handlers
**קובץ:** `scripts/error-handlers.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### event-handler-manager
**קובץ:** `scripts/event-handler-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### executions
**קובץ:** `scripts/executions.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### external-data-dashboard
**קובץ:** `scripts/external-data-dashboard.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ✅ כן
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 161:** פונקציית פורמט מקומית: formatNumber
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatNumber(`

---

### external-data-service
**קובץ:** `scripts/external-data-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### external-data-settings-service
**קובץ:** `scripts/external-data-settings-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### global-favicon
**קובץ:** `scripts/global-favicon.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### header-system-old
**קובץ:** `scripts/header-system-old.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### header-system-v2
**קובץ:** `scripts/header-system-v2.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### header-system
**קובץ:** `scripts/header-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### history-widget
**קובץ:** `scripts/history-widget.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### icon-mappings
**קובץ:** `scripts/icon-mappings.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### icon-replacement-helper
**קובץ:** `scripts/icon-replacement-helper.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### icon-system
**קובץ:** `scripts/icon-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### import-user-data
**קובץ:** `scripts/import-user-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 3130:** מפת תרגום מקומית: statusMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `const statusMap = {`

2. **שורה 3130:** מפת תרגום מקומית: statusMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `statusMap = {`

3. **שורה 2185:** פונקציית פורמט מקומית: formatNumber
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatNumber(`

4. **שורה 2193:** פונקציית פורמט מקומית: formatAmount
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatAmount(`

---

### index
**קובץ:** `scripts/index.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### info-summary-configs
**קובץ:** `scripts/info-summary-configs.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### info-summary-system
**קובץ:** `scripts/info-summary-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### all-pages-monitoring-test
**קובץ:** `scripts/init-system/all-pages-monitoring-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### comprehensive-initialization-test
**קובץ:** `scripts/init-system/comprehensive-initialization-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### dependency-analyzer
**קובץ:** `scripts/init-system/dependency-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### code-cleanup-analyzer
**קובץ:** `scripts/init-system/dev-tools/code-cleanup-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### final-validator
**קובץ:** `scripts/init-system/dev-tools/final-validator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### page-template-generator
**קובץ:** `scripts/init-system/dev-tools/page-template-generator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### performance-analyzer
**קובץ:** `scripts/init-system/dev-tools/performance-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### script-analyzer
**קובץ:** `scripts/init-system/dev-tools/script-analyzer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### initialization-checker
**קובץ:** `scripts/init-system/initialization-checker.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### load-order-validator
**קובץ:** `scripts/init-system/load-order-validator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### package-manifest
**קובץ:** `scripts/init-system/package-manifest.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### page-health-checker
**קובץ:** `scripts/init-system/page-health-checker.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### pages-standardization-plan
**קובץ:** `scripts/init-system/pages-standardization-plan.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### validate-package-manifest
**קובץ:** `scripts/init-system/validate-package-manifest.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### runtime-validator
**קובץ:** `scripts/init-system/validators/runtime-validator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### init-system-check
**קובץ:** `scripts/init-system-check.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### init-system-management
**קובץ:** `scripts/init-system-management.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### linked-items
**קובץ:** `scripts/linked-items.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 1037:** מפת תרגום מקומית: typeMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `const typeMap = {`

2. **שורה 1102:** מפת תרגום מקומית: typeMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `const typeMap = {`

3. **שורה 1037:** מפת תרגום מקומית: typeMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `typeMap = {`

4. **שורה 1102:** מפת תרגום מקומית: typeMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `typeMap = {`

---

### linter-realtime-monitor
**קובץ:** `scripts/linter-realtime-monitor.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 2031:** מפת תרגום מקומית: typeMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `const typeMap = {`

2. **שורה 2031:** מפת תרגום מקומית: typeMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `typeMap = {`

---

### log-recovery
**קובץ:** `scripts/log-recovery.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### logger-service
**קובץ:** `scripts/logger-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### migration-helper
**קובץ:** `scripts/migration-helper.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### migration-testing-suite
**קובץ:** `scripts/migration-testing-suite.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### economic-events-mock-data
**קובץ:** `scripts/mock-data/economic-events-mock-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trade-history-mock-data
**קובץ:** `scripts/mock-data/trade-history-mock-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### mockups-icon-initializer
**קובץ:** `scripts/mockups-icon-initializer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### alerts-config
**קובץ:** `scripts/modal-configs/alerts-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cash-flows-config
**קובץ:** `scripts/modal-configs/cash-flows-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### conditions-config
**קובץ:** `scripts/modal-configs/conditions-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### executions-config
**קובץ:** `scripts/modal-configs/executions-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notes-config
**קובץ:** `scripts/modal-configs/notes-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tag-management-config
**קובץ:** `scripts/modal-configs/tag-management-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tag-search-config
**קובץ:** `scripts/modal-configs/tag-search-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tickers-config
**קובץ:** `scripts/modal-configs/tickers-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trade-plans-config
**קובץ:** `scripts/modal-configs/trade-plans-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trades-config
**קובץ:** `scripts/modal-configs/trades-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trading-accounts-config
**קובץ:** `scripts/modal-configs/trading-accounts-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### modal-manager-v2
**קובץ:** `scripts/modal-manager-v2.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### modal-navigation-manager
**קובץ:** `scripts/modal-navigation-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### modal-navigation-monitor
**קובץ:** `scripts/modal-navigation-monitor.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### actions-menu-system
**קובץ:** `scripts/modules/actions-menu-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### communication-module
**קובץ:** `scripts/modules/communication-module.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### core-systems
**קובץ:** `scripts/modules/core-systems.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### data-advanced
**קובץ:** `scripts/modules/data-advanced.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### dynamic-loader-config
**קובץ:** `scripts/modules/dynamic-loader-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### localstorage-sync
**קובץ:** `scripts/modules/localstorage-sync.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notification-templates
**קובץ:** `scripts/modules/notification-templates.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### ui-advanced
**קובץ:** `scripts/modules/ui-advanced.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### ui-basic
**קובץ:** `scripts/modules/ui-basic.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 272:** פונקציית פורמט מקומית: colorAmount
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function colorAmount(`

---

### monitor-import-execution
**קובץ:** `scripts/monitor-import-execution.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### monitoring-functions
**קובץ:** `scripts/monitoring-functions.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notes
**קובץ:** `scripts/notes.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ✅ כן
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 2525:** פונקציה מקומית לתרגום: getTypeDisplayName
   - **סוג:** localTranslationFunction
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `function getTypeDisplayName(`

2. **שורה 2490:** מפת תרגום מקומית: typeMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `const typeMap = {`

3. **שורה 2490:** מפת תרגום מקומית: typeMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `typeMap = {`

---

### notification-category-detector
**קובץ:** `scripts/notification-category-detector.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notification-system-tester
**קובץ:** `scripts/notification-system-tester.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notification-system
**קובץ:** `scripts/notification-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notifications-center
**קובץ:** `scripts/notifications-center.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### page-initialization-configs
**קובץ:** `scripts/page-initialization-configs.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### page-state-e2e-test
**קובץ:** `scripts/page-state-e2e-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### page-state-manager
**קובץ:** `scripts/page-state-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### page-utils
**קובץ:** `scripts/page-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### pagination-system
**קובץ:** `scripts/pagination-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### pending-execution-trade-creation
**קובץ:** `scripts/pending-execution-trade-creation.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### pending-executions-widget
**קובץ:** `scripts/pending-executions-widget.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### pending-trade-plan-widget
**קובץ:** `scripts/pending-trade-plan-widget.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### portfolio-state-page
**קובץ:** `scripts/portfolio-state-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 1444:** פונקציית פורמט מקומית: formatCurrency
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatCurrency(`

---

### positions-portfolio
**קובץ:** `scripts/positions-portfolio.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-colors
**קובץ:** `scripts/preferences-colors.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-core-new
**קובץ:** `scripts/preferences-core-new.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-debug-monitor
**קובץ:** `scripts/preferences-debug-monitor.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-group-manager
**קובץ:** `scripts/preferences-group-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-lazy-loader
**קובץ:** `scripts/preferences-lazy-loader.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-page
**קובץ:** `scripts/preferences-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-pages-scanner
**קובץ:** `scripts/preferences-pages-scanner.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-profiles
**קובץ:** `scripts/preferences-profiles.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-refresh-controls
**קובץ:** `scripts/preferences-refresh-controls.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-refresh-monitor
**קובץ:** `scripts/preferences-refresh-monitor.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-ui-v4
**קובץ:** `scripts/preferences-ui-v4.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-ui
**קובץ:** `scripts/preferences-ui.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-validation
**קובץ:** `scripts/preferences-validation.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences
**קובץ:** `scripts/preferences.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### price-history-page
**קובץ:** `scripts/price-history-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### quick-quality-check
**קובץ:** `scripts/quick-quality-check.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### realtime-notifications-client
**קובץ:** `scripts/realtime-notifications-client.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### register-table-portfolio
**קובץ:** `scripts/register-table-portfolio.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### related-object-filters
**קובץ:** `scripts/related-object-filters.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### research
**קובץ:** `scripts/research.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### server-monitor
**קובץ:** `scripts/server-monitor.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### alert-condition-renderer
**קובץ:** `scripts/services/alert-condition-renderer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### alerts-data
**קובץ:** `scripts/services/alerts-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cash-flows-data
**קובץ:** `scripts/services/cash-flows-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### crud-response-handler
**קובץ:** `scripts/services/crud-response-handler.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 753:** מפת תרגום מקומית: actionMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `const actionMap = {`

2. **שורה 753:** מפת תרגום מקומית: actionMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `actionMap = {`

---

### dashboard-data
**קובץ:** `scripts/services/dashboard-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### data-collection-service
**קובץ:** `scripts/services/data-collection-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### data-import-data
**קובץ:** `scripts/services/data-import-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### default-value-setter
**קובץ:** `scripts/services/default-value-setter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### executions-data
**קובץ:** `scripts/services/executions-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### field-renderer-service
**קובץ:** `scripts/services/field-renderer-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ✅ כן
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 425:** פונקציית פורמט מקומית: formatAmount
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `const formatAmount =`

---

### investment-calculation-service
**קובץ:** `scripts/services/investment-calculation-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 289:** פונקציית פורמט מקומית: formatAmount
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatAmount(`

---

### linked-items-service
**קובץ:** `scripts/services/linked-items-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### lint-status-service
**קובץ:** `scripts/services/lint-status-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### notes-data
**קובץ:** `scripts/services/notes-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-data
**קובץ:** `scripts/services/preferences-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### preferences-v4
**קובץ:** `scripts/services/preferences-v4.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### research-data
**קובץ:** `scripts/services/research-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### rich-text-editor-service
**קובץ:** `scripts/services/rich-text-editor-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### select-populator-service
**קובץ:** `scripts/services/select-populator-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### statistics-calculator
**קובץ:** `scripts/services/statistics-calculator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### table-sort-value-adapter
**קובץ:** `scripts/services/table-sort-value-adapter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tag-service
**קובץ:** `scripts/services/tag-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tickers-data
**קובץ:** `scripts/services/tickers-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trade-plans-data
**קובץ:** `scripts/services/trade-plans-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trades-data
**קובץ:** `scripts/services/trades-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trading-accounts-data
**קובץ:** `scripts/services/trading-accounts-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### unified-crud-service
**קובץ:** `scripts/services/unified-crud-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### services-test
**קובץ:** `scripts/services-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### standardize-pages
**קובץ:** `scripts/standardize-pages.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### strategy-analysis-page
**קובץ:** `scripts/strategy-analysis-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 1270:** פונקציית פורמט מקומית: formatCurrency
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatCurrency(`

---

### sm-base
**קובץ:** `scripts/system-management/core/sm-base.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-error-handler
**קובץ:** `scripts/system-management/core/sm-error-handler.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-ui-components
**קובץ:** `scripts/system-management/core/sm-ui-components.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-section-alerts
**קובץ:** `scripts/system-management/sections/sm-section-alerts.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-section-background-tasks
**קובץ:** `scripts/system-management/sections/sm-section-background-tasks.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-section-cache
**קובץ:** `scripts/system-management/sections/sm-section-cache.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-section-dashboard
**קובץ:** `scripts/system-management/sections/sm-section-dashboard.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-section-database
**קובץ:** `scripts/system-management/sections/sm-section-database.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-section-external-data
**קובץ:** `scripts/system-management/sections/sm-section-external-data.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-section-operations
**קובץ:** `scripts/system-management/sections/sm-section-operations.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-section-performance
**קובץ:** `scripts/system-management/sections/sm-section-performance.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### sm-section-server
**קובץ:** `scripts/system-management/sections/sm-section-server.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### system-management-main
**קובץ:** `scripts/system-management/system-management-main.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### system-management
**קובץ:** `scripts/system-management.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### table-data-registry
**קובץ:** `scripts/table-data-registry.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### table-mappings
**קובץ:** `scripts/table-mappings.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tables
**קובץ:** `scripts/tables.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tag-events
**קובץ:** `scripts/tag-events.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tag-management-page
**קובץ:** `scripts/tag-management-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tag-search-controller
**קובץ:** `scripts/tag-search-controller.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tag-ui-manager
**קובץ:** `scripts/tag-ui-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-db-display-page-loading
**קובץ:** `scripts/test-db-display-page-loading.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-executions-page-loading
**קובץ:** `scripts/test-executions-page-loading.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-monitoring
**קובץ:** `scripts/test-monitoring.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-positions-portfolio-comprehensive
**קובץ:** `scripts/test-positions-portfolio-comprehensive.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-preferences-debug
**קובץ:** `scripts/test-preferences-debug.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-preferences-loading-across-pages
**קובץ:** `scripts/test-preferences-loading-across-pages.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-preferences-loading-complete
**קובץ:** `scripts/test-preferences-loading-complete.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-runner
**קובץ:** `scripts/test-runner.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test-unified-table-system
**קובץ:** `scripts/test-unified-table-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### comprehensive-cache-clearing-test
**קובץ:** `scripts/testing/comprehensive-cache-clearing-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test_cache_system_integration
**קובץ:** `scripts/testing/test_cache_system_integration.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test_frontend_wrappers
**קובץ:** `scripts/testing/test_frontend_wrappers.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test_initialization_stages
**קובץ:** `scripts/testing/test_initialization_stages.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test_packages_and_page_configs
**קובץ:** `scripts/testing/test_packages_and_page_configs.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### test_preferences_loading_events
**קובץ:** `scripts/testing/test_preferences_loading_events.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### debug-cashflow-account
**קובץ:** `scripts/tests/debug-cashflow-account.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### edit-modal-test
**קובץ:** `scripts/tests/edit-modal-test.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### field-mapping-validator
**קובץ:** `scripts/tests/field-mapping-validator.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### ticker-service
**קובץ:** `scripts/ticker-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tickers
**קובץ:** `scripts/tickers.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 3045:** פונקציה מקומית לתרגום: getTypeDisplayName
   - **סוג:** localTranslationFunction
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `function getTypeDisplayName(`

---

### trade-history-page
**קובץ:** `scripts/trade-history-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trade-plan-service
**קובץ:** `scripts/trade-plan-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ✅ כן
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 140:** מפת תרגום מקומית: statusMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `const statusMap = {`

2. **שורה 140:** מפת תרגום מקומית: statusMap
   - **סוג:** localTranslationMap
   - **חומרה:** 🔴 גבוהה
   - **קוד:** `statusMap = {`

---

### trade-selector-modal
**קובץ:** `scripts/trade-selector-modal.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trades-adapter
**קובץ:** `scripts/trades-adapter.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trading-journal-page
**קובץ:** `scripts/trading-journal-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### trading_accounts
**קובץ:** `scripts/trading_accounts.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ✅ כן
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tradingview-test-page
**קובץ:** `scripts/tradingview-test-page.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tradingview-widgets-colors
**קובץ:** `scripts/tradingview-widgets/tradingview-widgets-colors.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tradingview-widgets-config
**קובץ:** `scripts/tradingview-widgets/tradingview-widgets-config.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tradingview-widgets-core
**קובץ:** `scripts/tradingview-widgets/tradingview-widgets-core.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### tradingview-widgets-factory
**קובץ:** `scripts/tradingview-widgets/tradingview-widgets-factory.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### ui-utils
**קובץ:** `scripts/ui-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 350:** פונקציית פורמט מקומית: colorAmount
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function colorAmount(`

---

### unified-app-initializer
**קובץ:** `scripts/unified-app-initializer.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### unified-cache-manager
**קובץ:** `scripts/unified-cache-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### unified-log-api
**קובץ:** `scripts/unified-log-api.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### unified-log-display
**קובץ:** `scripts/unified-log-display.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### unified-log-manager
**קובץ:** `scripts/unified-log-manager.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### business-logic-batch-helper
**קובץ:** `scripts/utils/business-logic-batch-helper.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### cache-key-helper
**קובץ:** `scripts/utils/cache-key-helper.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### request-deduplication-helper
**קובץ:** `scripts/utils/request-deduplication-helper.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### validation-utils
**קובץ:** `scripts/validation-utils.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### warning-system
**קובץ:** `scripts/warning-system.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

### recent-trades-widget
**קובץ:** `scripts/widgets/recent-trades-widget.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ⚠️ כן

#### סטיות שנמצאו:

1. **שורה 69:** פונקציית פורמט מקומית: formatAmount
   - **סוג:** localFormatFunction
   - **חומרה:** 🟡 בינונית
   - **קוד:** `function formatAmount(`

---

### yahoo-finance-service
**קובץ:** `scripts/yahoo-finance-service.js`
**קטגוריה:** JavaScript

#### סטטוס:
- **משתמש במערכת:** ❌ לא
- **טוען את המערכת:** ❌ לא (נטען דרך base package)
- **יש בעיות:** ✅ לא

#### ✅ אין סטיות - הקובץ לא משתמש בתרגום

---

## 🎯 סיכום והמלצות

### בעיות קריטיות (גבוהה חומרה):
- **scripts/cluster-table.js** - 2 בעיות
- **scripts/database.js** - 2 בעיות
- **scripts/import-user-data.js** - 2 בעיות
- **scripts/linked-items.js** - 4 בעיות
- **scripts/linter-realtime-monitor.js** - 2 בעיות
- **scripts/notes.js** - 3 בעיות
- **scripts/services/crud-response-handler.js** - 2 בעיות
- **scripts/tickers.js** - 1 בעיות
- **scripts/trade-plan-service.js** - 2 בעיות

### בעיות בינוניות:
- **scripts/account-activity.js** - 1 בעיות
- **scripts/cash_flows.js** - 1 בעיות
- **scripts/comparative-analysis-page.js** - 1 בעיות
- **scripts/database.js** - 2 בעיות
- **scripts/date-comparison-modal.js** - 1 בעיות
- **scripts/entity-details-renderer.js** - 1 בעיות
- **scripts/external-data-dashboard.js** - 1 בעיות
- **scripts/import-user-data.js** - 2 בעיות
- **scripts/modules/ui-basic.js** - 1 בעיות
- **scripts/portfolio-state-page.js** - 1 בעיות
- **scripts/services/field-renderer-service.js** - 1 בעיות
- **scripts/services/investment-calculation-service.js** - 1 בעיות
- **scripts/strategy-analysis-page.js** - 1 בעיות
- **scripts/ui-utils.js** - 1 בעיות
- **scripts/widgets/recent-trades-widget.js** - 1 בעיות

---

**תאריך עדכון אחרון:** 26.11.2025, 3:53:16
