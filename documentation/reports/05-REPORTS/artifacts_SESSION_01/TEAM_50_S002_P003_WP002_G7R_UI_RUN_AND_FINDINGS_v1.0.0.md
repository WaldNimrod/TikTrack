# TEAM_50 → TEAM_10 | S002-P003-WP002 — ריצת בדיקות וממצאי ממשק

**תאריך:** 2026-03-06  
**סטטוס:** ריצה בוצעה; יש דיווח על בעיות ממשק נותרות.  
**מטרה:** תיעוד ריצת סוויטות + רישום בעיות ברמת הממשק לצורך תיקון (Team 30) או הרחבת בדיקות (Team 50).

---

## 1) מה הורץ

| ריצה | פקודה | תוצאה |
|------|--------|--------|
| 26-BF E2E | `node tests/g7-26bf-e2e-validation.test.js` | **25 PASS, 1 FAIL** (ראו להלן) |
| Deep E2E | `node tests/g7-26bf-deep-e2e.test.js` | **לא הורץ** — ChromeDriver לא זמין (ECONNREFUSED 127.0.0.1:9515). להפעיל chromedriver על פורט 9515 או להריץ ללא `CHROMEDRIVER_REMOTE` אם ה-driver מקומי. |

---

## 2) תוצאות 26-BF E2E (מהריצה)

- **PASS (25):** 001–023, 025, 026 (favicon, entity, validation, filters, tooltips, ביטול, modal, API evidence ל־008–011, טבלאות התראות/הערות, condition, אין general, rich text, pagination, קבצים inline, 2.5MB, refresh).
- **FAIL (1):** **BF-G7-024** — תצוגה/פתיחה/הורדה של קבצים בפרטי הערה. ב־E2E לא נפתחת הערה עם קבצים, ולכן לא אומתה בפועל ברמת הממשק. בקוד: `notesTableInit.buildAttachmentsHtml` (פתח/הורד).

**ארטיפקט:** `TEAM_50_G7_26BF_E2E_RESULTS.json`

---

## 3) בעיות בממשק (דיווח משתמש — רשימה מלאה)

ממצאים כפי שדווחו. **נתיב לתיקון:** Team 30 (ממשק). Team 10 לנתב לפי הטבלה להלן.

| # | עמוד / תהליך | תיאור (נוכחי) | צפוי | חוסם? |
|---|----------------|----------------|------|--------|
| 1 | **כל העמודים הרלוונטיים** — עמודת "אלמנט מקושר" | כרגע: איקון + סוג בלבד. | להציג **גם את הרשומה הספציפית** (שם/סמל) **וכולל קישור** למודול הפרטים של הרשומה המקושרת. | כן |
| 2 | **D35 הערות** — קובץ מצורף | קובץ מצורף לא מוצג **לא בטבלה** **ולא במודול פרטים**. | להציג קבצים מצורפים בטבלה (עמודה/אינדיקציה) ובמודול פרטי הערה (רשימה + פתח/הורד). | כן |
| 3 | **כל הטבלאות** — אחרי עדכון רשומה | **הכי חמור:** אחרי ביצוע עדכון לרשומה — **הטבלה לא מתעדכנת** במידע החדש. | אחרי שמירת עריכה — הטבלה בעמוד תציג את הנתונים המעודכנים **מיד** (ללא ריענון דף). | כן |
| 4 | **D35** — `id="notesSummaryToggleSize"` | לא מיושר כמו שצריך. | הנתונים **במרכז**, הכפתור **בצד שמאל** — כמו בעמוד טיקרים: השעון במיקום הנכון, כפתור העין בצד שמאל. | לא |
| 5 | **פילטרים + תפריט פעולות** | **אין טולטיפים** — לא בפילטרים לפי סוג מקושר, לא בתפריט הפעולות בטבלה. | להוסיף `title`/טולטיפים לפילטרים (סוג מקושר) ולתפריט הפעולות (עריכה, מחיקה וכו'). | לא |
| 6 | **D35 הוספת הערה** — ממשק קישור | מציג **"אופציונלי"** ולא מאפשר קישור לישות **ספציפית**. | קישור **חובה** לישות ספציפית (לא אופציונלי); בחירת ישות מקושרת ספציפית (דרופדאון/חיפוש לפי טיקר/חשבון/וכו'). | כן |
| 7 | **D34 הוספת התראה** — ממשק קישור | שני הדרופדאונים צמודים ולא יפים. | להציג את שדות הקישור **בשתי עמודות** (layout נקי). | לא |
| 8 | **כפתורי ביטול ושמירה** (מודלים) | כפתורי ביטול ושמירה תמיד בצבע אישור/שגיאה. | ביטול — **לא** צבע אישור (למשל secondary/neutral); שמירה — צבע ראשי. עקביות עם עיצוב המערכת. | לא |

**סיכום:** חוסמים: **1, 2, 3, 6**. לא חוסמים אך נדרשים: 4, 5, 7, 8.

---

## 4) המלצות להמשך

1. **נתיב ל־Team 30:** מסמך הניתוב המלא עם שמונת הממצאים והנחיות טכניות: **`TEAM_50_TO_TEAM_10_UI_FINDINGS_ROUTING_v1.0.0.md`**. ארבעה חוסמים (1, 2, 3, 6) — לעדכן מיד; ארבעה שיפורים (4, 5, 7, 8) — כפי יכולת.
2. **הרצת Deep E2E:** להפעיל ChromeDriver (למשל `chromedriver --port=9515`) ואז להריץ `node tests/g7-26bf-deep-e2e.test.js` — כולל בדיקת סמל תקין, 017, 026 (רענון טבלה). לאחר תיקון ממצא 3 — לוודא ש־026 עובר.
3. **אימות חוזר:** לאחר תיקוני Team 30 — להריץ שוב את סוויטות 26-BF ו־Deep E2E ולוודא שממצאים 1–8 אינם חוזרים.

---

**log_entry | TEAM_50 | S002_P003_WP002_G7R_UI_RUN_AND_FINDINGS | RUN_DONE_1_FAIL_UI_ISSUES_REPORTED | 2026-03-06**
