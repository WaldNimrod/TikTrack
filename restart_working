#!/bin/bash

# TikTrack Working Restart Script
# ================================
# 
# 🎯 Purpose: Start server and return control immediately
# ⚡ Features: No waiting, no health checks, instant return
# 📖 Based on: start_dev.sh working approach

set -e

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

# Logging
log_info() {
    echo -e "${BLUE}[$(date '+%H:%M:%S')] INFO:${NC} $1"
}

log_success() {
    echo -e "${GREEN}[$(date '+%H:%M:%S')] SUCCESS:${NC} $1"
}

log_warning() {
    echo -e "${YELLOW}[$(date '+%H:%M:%S')] WARNING:${NC} $1"
}

log_error() {
    echo -e "${RED}[$(date '+%H:%M:%S')] ERROR:${NC} $1"
}

# Function to set cache mode
set_cache_mode() {
    local mode="$1"
    
    case $mode in
        development)
            export TIKTRACK_DEV_MODE="true"
            export TIKTRACK_CACHE_DISABLED="false"
            log_info "Cache mode: DEVELOPMENT"
            ;;
        no-cache)
            export TIKTRACK_DEV_MODE="true"
            export TIKTRACK_CACHE_DISABLED="true"
            log_info "Cache mode: NO-CACHE"
            ;;
        production)
            export TIKTRACK_DEV_MODE="false"
            export TIKTRACK_CACHE_DISABLED="false"
            log_info "Cache mode: PRODUCTION"
            ;;
        preserve)
            log_info "Cache mode: PRESERVE"
            ;;
        *)
            log_error "Invalid cache mode: $mode"
            exit 1
            ;;
    esac
}

# Function to stop server
stop_server() {
    log_info "Stopping server..."
    
    # Kill processes using port 8080
    if lsof -i :8080 >/dev/null 2>&1; then
        lsof -ti :8080 | xargs kill -9 2>/dev/null || true
        sleep 1
    fi
    
    log_success "Server stopped"
}

# Function to start server
start_server() {
    log_info "Starting server..."
    
    # Check if Backend directory exists
    if [ ! -d "Backend" ]; then
        log_error "Backend directory not found"
        exit 1
    fi
    
    # Enter Backend directory
    cd Backend
    
    # Start server with environment variables
    TIKTRACK_DEV_MODE="$TIKTRACK_DEV_MODE" \
    TIKTRACK_CACHE_DISABLED="$TIKTRACK_CACHE_DISABLED" \
    nohup python3 dev_server.py > /dev/null 2>&1 &
    
    local server_pid=$!
    cd ..
    
    log_success "Server started with PID: $server_pid"
    log_info "Server is starting in background..."
    log_info "Control returned immediately - no waiting!"
}

# Main function
main() {
    local cache_mode="preserve"
    
    # Parse arguments
    while [[ $# -gt 0 ]]; do
        case $1 in
            --cache-mode=*)
                cache_mode="${1#*=}"
                shift
                ;;
            *)
                log_warning "Unknown option: $1. Ignoring."
                shift
                ;;
        esac
    done
    
    echo "🔄 TikTrack Working Restart"
    echo "==========================="
    echo "📅 Started at: $(date '+%H:%M:%S')"
    echo "🎯 Cache Mode: $cache_mode"
    echo "⚡ Approach: Instant return (no waiting)"
    echo ""
    
    # Set cache mode
    set_cache_mode "$cache_mode"
    echo ""
    
    # Stop server
    stop_server
    echo ""
    
    # Start server
    start_server
    echo ""
    
    log_success "🎉 Restart completed!"
    echo "📊 Summary:"
    echo "   - Server started in background"
    echo "   - Control returned immediately"
    echo "   - No waiting for server to be ready"
    echo "🌐 Server will be available at: http://127.0.0.1:8080"
    echo "💡 Server may take up to 80 seconds to fully initialize"
    echo ""
    
    exit 0
}

# Run main function
main "$@"