# דוח פונקציות לא בשימוש - קובץ tickers.js

## תאריך הבדיקה
26 באוגוסט 2025

## מטרת הבדיקה
זיהוי פונקציות בקובץ `trading-ui/scripts/tickers.js` שלא נקראות בשום מקום בקוד הפעיל (ללא ספריות גיבוי).

## שיטת הבדיקה
- חיפוש פונקציות בקובץ tickers.js
- בדיקת קריאות לכל פונקציה בקבצי HTML ו-JavaScript
- התעלמות מספריות גיבוי (backups) וקבצים ישנים (2025)
- ספירת מספר הקריאות לכל פונקציה

## תוצאות הבדיקה

### פונקציות שלא בשימוש (0 קריאות)

| שם הפונקציה | תיאור קצר | שורה בקובץ | סטטוס |
|------------|----------|-----------|--------|
| `openTickerDetails(id)` | פתיחת פרטי טיקר | 229 | ❌ לא בשימוש |
| `editTicker(id)` | עריכת טיקר | 234 | ❌ לא בשימוש |
| `resetAllFiltersAndReloadData()` | איפוס פילטרים ורענון נתונים | 319 | ❌ לא בשימוש |
| `filterTickersLocally()` | פילטור מקומי של טיקרים | 1019 | ❌ לא בשימוש |
| `showTickerLinkedItemsModal()` | הצגת מודל פריטים מקושרים לטיקר | לא נמצאה | ❌ לא בשימוש |
| `goToLinkedItems()` | מעבר לפריטים מקושרים | לא נמצאה | ❌ לא בשימוש |
| `loadLinkedItemsDetails()` | טעינת פרטי פריטים מקושרים | לא נמצאה | ❌ לא בשימוש |
| `showSimpleLinkedItemsModal()` | הצגת מודל פשוט לפריטים מקושרים | לא נמצאה | ❌ לא בשימוש |

### פונקציות בשימוש חלקי (1-2 קריאות)

| שם הפונקציה | תיאור קצר | מספר קריאות | סטטוס |
|------------|----------|-------------|--------|
| `deleteTicker(id)` | מחיקת טיקר | 1 | ⚠️ שימוש מוגבל |
| `saveTicker()` | שמירת טיקר חדש | 1 | ⚠️ שימוש מוגבל |
| `updateTicker()` | עדכון טיקר קיים | 1 | ⚠️ שימוש מוגבל |
| `confirmDeleteTicker()` | אישור מחיקת טיקר | 2 | ⚠️ שימוש מוגבל |
| `viewLinkedItemsForTicker()` | הצגת פריטים מקושרים לטיקר | 2 | ⚠️ שימוש מוגבל |

### פונקציות בשימוש פעיל (3+ קריאות)

| שם הפונקציה | תיאור קצר | מספר קריאות | סטטוס |
|------------|----------|-------------|--------|
| `toggleTopSection()` | פתיחה/סגירה של סקשן עליון | 24 | ✅ בשימוש פעיל |
| `sortTable()` | מיון טבלה | 199 | ✅ בשימוש פעיל |
| `showFieldError()` | הצגת שגיאת שדה | 42 | ✅ בשימוש פעיל |
| `clearFieldError()` | ניקוי שגיאת שדה | 15 | ✅ בשימוש פעיל |
| `restoreSortState()` | שחזור מצב מיון | 7 | ✅ בשימוש פעיל |
| `loadCurrenciesData()` | טעינת נתוני מטבעות | 5 | ✅ בשימוש פעיל |
| `updateActiveTradesField()` | עדכון שדה טריידים פעילים | 3 | ✅ בשימוש פעיל |
| `loadLinkedItemsData()` | טעינת נתוני פריטים מקושרים | 3 | ✅ בשימוש פעיל |
| `displayLinkedItems()` | הצגת פריטים מקושרים | 2 | ✅ בשימוש פעיל |

## המלצות

### 1. פונקציות להסרה
הפונקציות הבאות מומלץ להסיר מהקוד:
- `openTickerDetails(id)` - לא בשימוש
- `editTicker(id)` - לא בשימוש  
- `resetAllFiltersAndReloadData()` - לא בשימוש
- `filterTickersLocally()` - לא בשימוש

### 2. פונקציות לבדיקה נוספת
הפונקציות הבאות דורשות בדיקה נוספת:
- `showTickerLinkedItemsModal()` - לא נמצאה בקובץ
- `goToLinkedItems()` - לא נמצאה בקובץ
- `loadLinkedItemsDetails()` - לא נמצאה בקובץ
- `showSimpleLinkedItemsModal()` - לא נמצאה בקובץ

### 3. פונקציות לשימור
הפונקציות הבאות חשובות ויש לשמור אותן:
- כל הפונקציות עם 3+ קריאות
- פונקציות ולידציה
- פונקציות CRUD בסיסיות

## הערות נוספות

1. **פונקציות חסרות**: חלק מהפונקציות שהוגדרו כגלובליות לא נמצאו בקובץ - ייתכן שהן הוסרו או הועברו לקבצים אחרים.

2. **שימוש בפונקציות גלובליות**: חלק מהפונקציות משתמשות בפונקציות גלובליות מ-`window` object.

3. **תלות בפונקציות חיצוניות**: הקובץ תלוי בפונקציות מקבצים אחרים כמו `linked-items.js`, `ui-utils.js` ועוד.

## סיכום

נמצאו **8 פונקציות לא בשימוש** בקובץ `tickers.js` שניתן להסיר בבטחה.
מומלץ לבצע ניקוי קוד ולהסיר את הפונקציות הלא נחוצות כדי לשפר את התחזוקתיות של הקוד.

---
*דוח זה נוצר אוטומטית על ידי כלי ניתוח קוד*
